FINAL HEAD ORDER: 623, 712, 713, 701, 707, 705, 708, 553 (662 → merged into 553), 596, 597, 706, 710 — then the user's 18.08 block 697, 703, 595/598, 581, 336 — then 715 (attended-gated, → moved behind the block).

Conflicts resolved:
- 712 front (B2) vs second (A2): took A — 623 keeps first (HIGH, asked twice, 18.08 re-ask is the freshest user word, requested-fix rule); 712 lands second, so its picker enforcement governs every later pick at the cost of exactly one.
- 705 merge into 708 (A7) vs separate-before-708 (B7): took B — the staged publish is a primitive three of 708's four commands will call, dependency-ordered work, not one work; A's transaction semantics stand as a binding build note on 708.
- 708 after 706 (A6) vs 708 ahead of low bookkeeping (B6): took B on the user's 17.08 word (per-point token overhead outranks low bookkeeping); A's primitive-dependency concern carried into 708's build note (U7).
- 662 merge into 553 (A11) vs keep-beside with paired landing (B11/B18): took A — one spec must reconcile the competing ceilings, and B's own verified fact (context now readable, point 700's fence) settles the reconciliation; B's substance retained (recalibrate on the measured context, keep the un-ticked-merge trigger), B18's single review comes free with the merge.
- 715 keep (A10) vs move out of the headless head (B10): took B — attended-only by its own spec (protected settings.json), it would jam the front-three picker; moved behind the 18.08 block and attended-gated; A's "substantive, not DONE" stands.
- Order line (A15 vs B15): B15's lever-before-bookkeeping refinement adopted (declared, on the user's 17.08 word); 706/710 stay before the block, nothing the user pulled forward moves, only attended-gated 715 goes behind it, declared.
