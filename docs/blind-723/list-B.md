B1 | 623 | KEEP: Head place right — user-requested mechanism asked TWICE (10.08 and item 6's 18.08 re-ask), HIGH criticality, and verified undelivered (no `scripts/vdzk-answer.mjs`, no message-keyed review or retrospect rule in `decision-card-guard-core.mjs`); requested fixes rank before QA tickets.
B2 | 712 | MOVE: To the very front, before 623 — its pick-refusal and branch-counted slots govern every subsequent pick in this head (the priority "stated three times over three weeks"), so landing it first makes the rest of the queue self-enforcing; verified unbuilt (`batch-in-flight-core.mjs` still computes `slotsFree = limit - running`, agents not branches).
B3 | 713 | KEEP: Position 3 right, but the owed COUNTED blind merge of `local/blind-713/` (0 identical vs 56 candidate pairs, third-model merger) is a hard prerequisite before building; the defect is verified live — `board-core.mjs:369` still reads the FIRST now-card for focus, exactly the measured 17.08 failure.
B4 | 701 | KEEP: Right before the unbuilt levers it must judge — 553's and 662's ceilings are explicitly "calibrated from the cost data", so its place at 4 is load-bearing; note partial substrate exists (`measure-task-cost.mjs` already prints the phase split and `--tasks`), but the per-point ledger command and the per-lever firing/effectiveness reading do not.
B5 | 707 | KEEP: Correctly after 712 (it only CARRIES 712's refusals into the report); verified undelivered — `guide-brevity-guard.mjs` still checks only `.claude/batch-paused` with no `heldByOtherLiveOwner`, and `guard-preflight-core.mjs` still emits `not-judged` for the four guards.
B6 | 708 | KEEP: Medium, but it is per-point overhead on every session — genuine token reduction, rightly ahead of the low board-bookkeeping points; its build order (file-a-point first) is already frequency-justified in the spec.
B7 | 705 | MOVE: Before 708 — 708's end-turn command must end in exactly ONE publish, which IS 705's staged mode, so building 708 first hard-codes the coupled edit-publish it would immediately rework; also note the "lift `.claude/batch-paused`" sub-item is moot (marker verified gone, pause already lifted) and should be struck at build time.
B8 | 706 | KEEP: Low and correctly behind the mediums; verified still present (`parseSetArgs` in `board-queue-core.mjs` still buckets prose after `--estimate` with no duration check — the flag-refusal at line ~227 covers only `--`-prefixed values), but see B17 on its overlap with 713.
B9 | 710 | KEEP: Low bookkeeping, place fine — but the perishable half is urgent independent of rank: `local/multistep-analysis-17-08/` (multistep-A/B, union.json, verified present) is git-ignored and one checkout event from gone; commit the raw artifacts NOW as evidence, ahead of the mapping work (see B16).
B10 | 715 | MOVE: Out of the headless head — declared refinement of the user's ranking: the remaining work is ATTENDED-ONLY by its own spec (`.claude/settings.json` protected; verified 36 scripts still listed in `RELATIVE_WIRING_ROLLOUT`, settings still 39 relative vs 2 anchored lines), so at position 10 it would jam 712's front-three picker for every headless session; either move it behind the user's 18.08 block or mark it attended-gated so the picker skips it.
B11 | 662 | KEEP: Place beside 553 right; partial delivery noted, not DONE — landed point 700's context fence now BINDS at the 150k watermark, covering the ">150k carried for 14 h" path, but the un-ticked-merge boundary demand is untouched (verified: `batch-progress-guard.mjs` still says "Merge and tick it first") and the spec's "tokens the scripts cannot read" premise is stale — recalibrate its ceiling on the now-readable context measure when built.
B12 | 553 | KEEP: High and correctly adjacent to 662 — both extend `batch-progress-guard`'s legal-stop/boundary rules and should be built together (see B18); verified unbuilt (no `scripts/point-handoff.mjs`).
B13 | 596 | KEEP: Correctly after 553 (rides the same measurement machinery and medians); verified unbuilt per its own 17.08 branch reading, and the silence-window third trigger (the six-and-a-half-hour bleed) is the part with proven cost — do not let it slip out in a re-cut.
B14 | 597 | KEEP: Last of the head fits its LOW criticality; note only the unconditional bounded-output half is workable now — the standing-load/cache-prefix half is CONDITIONAL on point 599, which is not in this head, so the point should land its unconditional half without waiting.
B15 | order | MOVE: Refinement of the head against the user's 17.08 word (only token-reduction outranks landing branches): pull the token-reduction levers 662/553/596/597 ahead of the low bookkeeping 706/710, giving 712, 623, 713, 701, 707, 705, 708, 662, 553, 596, 597, 706, 710 — the user's 18.08 block (697, 703, 595/598, 581, 336) stays directly after the head's last point, nothing pulled past it.
B16 | order | KEEP: Cross-point evidence rescue — commit the git-ignored blind/analysis artifacts (`local/blind-713/` for 713, `local/multistep-analysis-17-08/` for 710) in one small evidence commit immediately, decoupled from when either point is worked; both points cite them and a checkout loss would discard two paid-for four-eyes stages.
B17 | order | KEEP: Partial subsumption, not a merge — 713's derived now-section substantially covers 706's third bullet (`queue <N>` unseating the now-card: a derived render re-creates the card), so land 713 first and shrink 706's third bullet to a regression test; 706's parser and rebuild-card halves stay its own work.
B18 | order | KEEP: Build 662 and 553 as one coordinated change to `batch-progress-guard` with a single cross-model mechanism review — two sequential reviews of the same guard's boundary rules is the review-round cost 701 will measure as a top item; the points stay separate numbers, only their landing is paired.
