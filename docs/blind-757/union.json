{
  "mergedBy": "Fable 5",
  "newDestinations": [
    "docs/batch-owner-runbook.md — the ONE owner/dispatcher-only document, served by batch-resume-hook.mjs (SessionStart) to the lock holder; absorbs every proposal for docs/handover-architecture.md, docs/release-runbook.md, docs/review-method.md (its operational half) and 'a dispatcher document'",
    "docs/tts-architecture.md — TTS/read-aloud and voice-pipeline mechanics (both lists proposed this name)",
    "docs/render-architecture.md — renderer init/fallback implementation detail (B3)"
  ],
  "entries": [
    { "id": "U1", "from": ["A39", "B1"], "defect": "CLAUDE.md §1: the design.md-authority rule is stated twice in consecutive sentences ('authoritative for all design questions' / 'sole source of the target state'); axis c; DROPPED as an in-place merge to one binding sentence, ~30 words" },
    { "id": "U2", "from": ["A36", "B2"], "defect": "CLAUDE.md §8: all three bullets restate §2's scope guardrails; axis c; DROPPED — delete §8, folding its one non-duplicate sentence ('not to be started until §7.1 is fully met') into §2 (A's form saves more than B's pointer and loses nothing), ~60 words" },
    { "id": "U3", "from": ["A40"], "defect": "CLAUDE.md §2 balance bullet: the debug-menu-adjustability restatement duplicates criterion 20 and design.md §21; axis c; DROPPED as duplicate (the central-balance.ts rule stays), ~35 words" },
    { "id": "U4", "from": ["A2"], "defect": "CLAUDE.md §3 renderer: the 'goal is to use modern hardware' rationale sentence; axis c; MOVED -> docs/rule-corpus-audit.md, ~25 words" },
    { "id": "U5", "from": ["B3"], "defect": "CLAUDE.md §3 renderer: R3F async-gl init and fallback implementation detail; axis c; MOVED -> docs/render-architecture.md, keeping the binding WebGPU-primary/WebGL2-fallback/TSL/no-Chrome-only rules and the localized fallback notice, ~95 words" },
    { "id": "U6", "from": ["A3"], "defect": "CLAUDE.md §3 last renderer bullet ('if WebGPU gets stuck, fall back to plain WebGL and record an open item'): DROPPED-NEEDS-RULING — the contingency contradicts the live point-204 rule that WebGPU is the everyday asserted lane; only the user can retire a written escape hatch, ~30 words" },
    { "id": "U7", "from": ["A1", "B4"], "defect": "CLAUDE.md §3 journal read-aloud: the why-history and mechanics (point 117 vs 100, ~15 s GPU saturation, warmupSpeech timing, HF-CDN caching, __ttsForceWasm, voice.mjs rAF probe, device-path detail); axis c; MOVED -> docs/tts-architecture.md, keeping the binding sentences (kokoro-js in a worker, WebGPU-Chromium/WASM-elsewhere, pre-warm exists, lazy-load never in startup chunks, English-only), ~190 words" },
    { "id": "U8", "from": ["A4", "B5"], "defect": "CLAUDE.md §4: the ASCII project tree; the lists disagree (A: drop, B: move to README) — DROPPED: a directory listing is not a rule and ls re-derives it; the design.md->docs/design-reference.md pointer survives as one line, ~50 words" },
    { "id": "U9", "from": ["A5"], "defect": "CLAUDE.md §4: the design.md-never-modified/update-together prose and the node_modules gitignore trivia; axis a; COVERED -> rule-echo-guard + doc-budget-guard (Stop chain) and the update-docs-on-change-requests memory — Stop-chain, so ONE binding clause stays ('design.md and this file update together on a change request'), only the elaboration and trivia go, ~40 words" },
    { "id": "U10", "from": ["A6", "B6"], "defect": "CLAUDE.md §5 test-architecture paragraph: suite composition, examples and the old->new coverage map; axis a+c; MOVED -> scripts/verify/README.md (which the paragraph already names), keeping the layer rule ('Vitest for anything assertable without a browser, Playwright only for scene/geometry/CSS/audio/screenshot; every feature adds a test on the right layer'), ~110 words" },
    { "id": "U11", "from": ["A7"], "defect": "CLAUDE.md §5 tier paragraph: the prose duplicate of §7.2's tier rule; axis a; COVERED -> scripts/verify/tiers.mjs pinned by tiers.test.mjs — the npm-script block stays, the duplicated prose goes, ~50 words" },
    { "id": "U12", "from": ["B7"], "defect": "CLAUDE.md §6 feature-branch opening: the dated decisions (22.07./28.07.2026) and durability narrative; axis c; MOVED -> docs/rule-corpus-audit.md, the branch-per-point rule stays, ~60 words" },
    { "id": "U13", "from": ["A8", "A9", "B8"], "defect": "CLAUDE.md §6 commit/push/rescue mechanics: rescue-format rules COVERED -> versioned commit-msg hook (pre-action, refuses each half without the other — safe as a pointer); push durability COVERED -> push-arrival-guard (Stop chain — the preventive sentence 'commit atomically and push the branch after every commit' STAYS, only the rationale goes); ~105 words" },
    { "id": "U14", "from": ["A11", "B9"], "defect": "CLAUDE.md §6 merge-verification/backend-sensitivity: the DOM-only worked example and mechanics; axis a; COVERED -> render-verify-guard + ci-status-guard (Stop chain — so the rule sentence and the isBackendSensitivePath function name STAY per the stagger rule; only the example and elaboration go), ~70 words" },
    { "id": "U15", "from": ["A10", "B10"], "defect": "CLAUDE.md §6 'THE MERGE ENDS THE BRANCH': the 28.07.2026 debris incident and 31-of-36 measurement; axis a+c; incident MOVED -> docs/rule-corpus-audit.md; the rule sentence STAYS (branch-hygiene-guard is Stop-chain backstop, not pre-action), ~45 words" },
    { "id": "U16", "from": ["A12", "B11"], "defect": "CLAUDE.md §6 cross-cutting paragraph: the 42-of-60 first-parent measurement and arming explanation; axis c; MOVED -> docs/rule-corpus-audit.md, the small-to-main/large-delegated distinction stays, ~55 words" },
    { "id": "U17", "from": ["A13"], "defect": "CLAUDE.md §6 'use worktree isolation for parallel agents'; axis a; COVERED -> worktree-reminder (PreToolUse on the Agent matcher, fires before the spawn — safe as a bare pointer), ~20 words" },
    { "id": "U18", "from": ["A14", "B12"], "defect": "CLAUDE.md §6 TASKS.md-main-only + work-order-split rules: consumer mechanics and the three-quarters cost rationale; axis a+c; rationale MOVED -> docs/rule-corpus-audit.md, mechanics COVERED -> tasks-archive-guard (Stop chain — the two binding sentences and the scripts/tasks-source.mjs pointer STAY), ~95 words" },
    { "id": "U19", "from": ["A15", "B13"], "defect": "CLAUDE.md §6 'the landing is ONE command': the step enumeration; the lists disagree (A: drop as the script's own output, B: move to owner doc) — MOVED -> docs/batch-owner-runbook.md (safer); the command line and 'the gate is mandatory after EVERY merge' STAY, ~60 words" },
    { "id": "U20", "from": ["A16", "B14"], "defect": "CLAUDE.md §6 release/tag mechanism: items 1-2 COVERED -> closing-guard (PreToolUse, genuinely denies the tag before it exists) + tags-only-on-request memory; items 3-4 (poc-tag mirroring, workflow_dispatch, URL verify) MOVED -> docs/batch-owner-runbook.md (absorbing A's version-release-process.md and B's release-runbook.md proposals), ~175 words" },
    { "id": "U21", "from": ["A17", "B15"], "defect": "CLAUDE.md §6 deployed-main judgment: rationale and the branch-preview exception; axis c; MOVED -> docs/batch-owner-runbook.md, keeping two sentences (merge when test-green and both-backend verified; the aesthetic 'good enough?' is asked separately against deployed main), ~85 words" },
    { "id": "U22", "from": ["A18", "B16"], "defect": "CLAUDE.md §6 maximal delegation: pool cap, slot accounting and owner-retained duties; axis a+b; cap/queue COVERED -> commission-guard (PreToolUse, refuses before the spawn), owner mechanics MOVED -> docs/batch-owner-runbook.md; the delegated agent's own obligations (worktree, verify, push, do-not-merge) STAY, ~75 words" },
    { "id": "U23", "from": ["A19", "B17"], "defect": "CLAUDE.md §6 delegation brief: resolver behavior and the ~1.8k-vs-~108k measurement; axis b+c; measurement MOVED -> docs/rule-corpus-audit.md, dispatcher detail MOVED -> docs/batch-owner-runbook.md; one line naming scripts/point-brief.mjs stays for the agent that must run it, ~100 words" },
    { "id": "U24", "from": ["A20", "B18"], "defect": "CLAUDE.md §6 context-boundary/lease/claim/launcher/chat-watcher block (the largest bullet): axis b; MOVED -> docs/batch-owner-runbook.md served by batch-resume-hook.mjs to the owner alone, with batch-progress-guard (Stop chain) naming the remedy command; B18's bare-pointer treatment of the context fence is CORRECTED per U25 — the fence's preventive text does not reduce to a pointer, ~325 words" },
    { "id": "U25", "from": ["A21"], "defect": "CLAUDE.md §6 context-fence sentences: VERIFIED — CONTEXT_FENCE_MODE_DEFAULT='observe' in context-watermark-core.mjs and context-fence-guard.mjs prints 'OBSERVE — THE FENCE IS DISARMED and refuses NOTHING'; the fence rule therefore KEEPS its preventive sentence in CLAUDE.md until point 747 arms it (only the mode mechanics MOVE -> docs/batch-owner-runbook.md); this correction overrides any bare-pointer proposal from either list, ~40 words" },
    { "id": "U26", "from": ["A22", "B19"], "defect": "CLAUDE.md §6 model policy: routing/escalation operation MOVED -> docs/batch-owner-runbook.md, dated reversals MOVED -> docs/rule-corpus-audit.md; the allowlist is COVERED -> model-guard (Stop chain) + commit-msg hook's model-trailer-gate (pre-action), so the preventive line 'every commit names its author model; Sonnet and Haiku pause the batch' STAYS, ~175 words" },
    { "id": "U27", "from": ["A23", "B20"], "defect": "CLAUDE.md §6 four-eyes bullet: anchoring rationale, cost discussion and the decorrelated-fallback passage; axis c; MOVED -> docs/rule-corpus-audit.md (not a new review-method.md); the normative two-mode definition (divergent=blind-parallel, convergent=artefact-before-rationale) STAYS since its guards fire only at Stop, ~135 words" },
    { "id": "U28", "from": ["B21"], "defect": "CLAUDE.md §6 blind-merge mechanics: id accounting and merger-eligibility operation; axis b; MOVED -> docs/batch-owner-runbook.md; disagreement noted — A23 keeps the counted-merge rule in place, so the ONE-LINE rule (counted merge by the third model via scripts/blind-merge.mjs) STAYS and only the operational detail moves (mechanism-review-guard fires only at Stop), ~60 words" },
    { "id": "U29", "from": ["A24", "B22"], "defect": "CLAUDE.md §6 voice-markup bullet: the marker list (authoritative in design.md §15, already cited) and the parser-to-worker file chain; axis a+c; chain MOVED -> docs/tts-architecture.md; keeps 'every journal text in both languages carries the §15 markup; display strips it, read-aloud turns it into prosody', ~50 words" },
    { "id": "U30", "from": ["A25", "B23"], "defect": "CLAUDE.md §6 answer-from-the-repository rule: points history and rationale MOVED -> docs/rule-corpus-audit.md; the second half is backed by container-ask-guard but that is Stop-chain, so ONE preventive clause ('never route the user through manual steps') STAYS rather than becoming a bare pointer, ~40 words" },
    { "id": "U31", "from": ["B24"], "defect": "CLAUDE.md §6 'after each major system, run the self-verification' bullet; axis c; DROPPED as a verbatim duplicate of mandatory §7.2, ~12 words" },
    { "id": "U32", "from": ["A26", "B25"], "defect": "CLAUDE.md §7.1 preamble: the two meta-paragraphs explaining WHY detail and evidence moved out; axis c; reasoning MOVED -> docs/rule-corpus-audit.md, keeping two lines (where detail/evidence live; a criterion changes with them in the same commit), ~135 words" },
    { "id": "U33", "from": ["A27", "A28", "B26", "B28"], "defect": "CLAUDE.md §7.1 criteria 1, 10, 11 and 18: the only four the point-555 treatment left unfinished; axis c; full wording (including 18's audit-gate/ALLOW-map/GHSA narrative) MOVED -> docs/acceptance-criteria-detail.md under the same numbers, each criterion reduced to number, title, one condition and the two pointers like its siblings, ~165 words" },
    { "id": "U34", "from": ["B27"], "defect": "CLAUDE.md §7.1 criteria 2-9 and 12-31: trim enumerated subfeatures to one short condition each, every removed requirement MOVED verbatim -> docs/acceptance-criteria-detail.md same-number sections (this JUDGES point 555's cut as not far enough without undoing it), ~430 words" },
    { "id": "U35", "from": ["B29"], "defect": "CLAUDE.md §7.1 criterion 32: the delivered-then-removed SSR history; axis c; MOVED -> docs/acceptance-criteria-detail.md §32, the current TRAA/refraction condition and pointers stay, ~35 words" },
    { "id": "U36", "from": ["A29", "B30"], "defect": "CLAUDE.md §7.2 first verification bullets: build/lint/audit/suite-composition repetition; axis a+c; COVERED -> pre-push-gate.mjs via the versioned pre-push hook (pre-action) with a tier-selection sentence pointing at §5 and scripts/verify/README.md, ~95 words" },
    { "id": "U37", "from": ["B31"], "defect": "CLAUDE.md §7.2 achievable-conditions paragraph (point 172): numeric zoom examples, fog explanation and incident references; axis c; MOVED -> scripts/verify/README.md, keeping the two binding rules (test at player-reachable states; judge by the rendered projection, never an assumed radius), ~90 words" },
    { "id": "U38", "from": ["A30", "B32"], "defect": "CLAUDE.md §7.2 frame-subject paragraph (point 375): shutter mechanics and taxonomy MOVED -> scripts/verify/README.md, the two-runs-photographed-different-places incident MOVED -> docs/rule-corpus-audit.md; safe to compress because frameSubject.mjs refuses the mis-subjected frame at the shutter (pre-action) and a unit gate catches out-of-shutter writes; one line with the subject vocabulary stays, ~90 words" },
    { "id": "U39", "from": ["A31", "B33"], "defect": "CLAUDE.md §7.2 backend-coverage subsection: launcher assertions, routing matrix and maintenance mechanics; axis a+c; MOVED -> scripts/verify/README.md, keeping the lane rules one-line ('WebGPU everyday, WebGL 2 the LARGE regression lane, touch/voice routed to WebGL 2') plus the RESIDUAL sentence, backed by assertBackend failing loud in-suite, ~160 words" },
    { "id": "U40", "from": ["A32", "B34", "B35", "B36", "B37"], "defect": "CLAUDE.md §7.2 Stop-chain paragraph (~420 words): the lists disagree on depth (A: three lines; B: one preventive sentence per family) — B's form is safer and wins for the Stop-fired families: KEEP one preventive sentence per family (BOARD, BATCH, WORK ORDER, FINDING, PROOF, including one line for ci-status-guard's every-pushed-ref scope), with .claude/settings.json named as the authoritative list; the board-first subsection reduces to a bare pointer (board-first-guard is PreToolUse, fires before the mutation); the closing-guard restatement reduces to a pointer to §9 (PreToolUse); the git-hook mechanics are COVERED -> the versioned pre-action hooks themselves (commit-msg/pre-commit/pre-push refuse before the action; scripts/git-hooks/README.md does NOT exist and is not created), keeping 'versioned hooks, wired by npm install'; incidents MOVED -> docs/rule-corpus-audit.md; ~300 words" },
    { "id": "U41", "from": ["A33", "B38"], "defect": "CLAUDE.md §7.2 guard-preflight paragraph: the ~30-turn loop measurement MOVED -> docs/rule-corpus-audit.md; the command and the answer-LAST rule STAY in CLAUDE.md (they bind every session's turn end, not only the owner — B38's axis-b move is rejected as the less safe account), ~55 words" },
    { "id": "U42", "from": ["A34", "B39"], "defect": "CLAUDE.md §7.2 screenshot-diffing paragraph: the 11-98 %/0.75 % replay measurements and rejection history; axis c; MOVED -> docs/picture-check-levers.md (which already holds the verdicts), keeping 'no golden-image gate until picture-stability.mjs reports STABLE', ~55 words" },
    { "id": "U43", "from": ["A35", "B40"], "defect": "CLAUDE.md §7.2 red-closes-by-a-CAUSE paragraph: --defer/waiver semantics and throttle-probe invocation detail; axis a+c; MOVED -> scripts/verify/README.md, keeping the three closings, 'a retried run is recorded SUSPECT and covers nothing', ~65 words" },
    { "id": "U44", "from": ["A37", "B41"], "defect": "CLAUDE.md §9 closing-completeness block: sequence implementation, point history and evidence syntax; axis a; COVERED -> closing-guard (PreToolUse, denies the tag and the tick before they happen — safe as a near-bare pointer to CLOSING_STEPS plus the --status/--step drive line); the v0.2 narrative MOVED -> docs/rule-corpus-audit.md, ~120 words" },
    { "id": "U45", "from": ["A38", "B42"], "defect": "CLAUDE.md §9 tail, two parts: (1) graphics detail-level reminder — DROPPED-NEEDS-RULING: qualityDoc.test.ts already proves it every unit run, but the user deliberately asked for it to be NAMED at the closing, so reducing it to a checklist clause needs his ruling; (2) the closing-freeze procedure — MOVED -> docs/batch-owner-runbook.md (owner-only, axis b), the one-line freeze rule staying in §9; ~85 words" },
    { "id": "U46", "from": ["A41", "B44"], "defect": "MEMORY.md: entries self-marked dead — sol-authors-by-default ('ENDED 17.08.2026') DELETED per the brief's explicit deletion ruling; B44's blanket deletion of tasks-time-tracking and webgpu-testable-headless is OVERRIDDEN by the safer treatments in U48 and U50, ~35 words" },
    { "id": "U47", "from": ["A44", "B45"], "defect": "MEMORY.md: fable-sparingly and provider-volume-strategy each carry a dead half ('DEAD: four-eyes model'; 'the OpenAI-first emergency ended') — the dead halves DELETED, one live clause kept each, ~40 words" },
    { "id": "U48", "from": ["A42"], "defect": "MEMORY.md tasks-time-tracking ('DEAD IN PRACTICE, awaiting the user's decision'): DROPPED-NEEDS-RULING — ask the user to kill the [*]/[~] states outright rather than pay for a dead mechanism every turn (B44 wanted outright deletion; the entry itself says the decision is the user's), ~25 words" },
    { "id": "U49", "from": ["A43"], "defect": "MEMORY.md parallel-session-root-cause: records a solved incident ('merged and verified'), not a live rule; DELETED, the incident kept in docs/batch-singleton-analysis.md, ~25 words" },
    { "id": "U50", "from": ["A45"], "defect": "MEMORY.md webgpu-untestable-headless: titled after the claim it corrects; REWRITTEN to one clause ('WebGPU IS testable via system Chrome --headless=new; only Playwright's bundled Chromium fails') with the topic file renamed — safer than B44's deletion, which would lose the live fact, ~25 words" },
    { "id": "U51", "from": ["A46", "B51"], "defect": "MEMORY.md findings-carrier bullet (854 B, the file's longest): path derivation, trailing-dash explanation and migration caveat MOVED -> its topic/carrier file, leaving the path, 'findings-guard blocks the turn end on a waiting entry', and the drain command; B51's pending-queue reduction is handled by the safer needs-ruling treatment in U55, ~145 words" },
    { "id": "U52", "from": ["A47"], "defect": "MEMORY.md guard-covered cluster (stay-within-project-dir, container-work-is-mine, chat-timestamp, dashboard-card-single-topic, dashboard-vdzk-only-decisions, dashboard-all-open-points-in-queue, bundle-first-not-new-point, queue-order-fixes-before-finders, always-prep-during-waits, retrospective-currency-mechanism, sort-visuals-into-detail-levels): each index line DELETED, its named guard (path-scope-guard PreToolUse; container-ask-guard, timestamp-guard, dashboard-card-topic-guard, decision-card-guard, dashboard-integrity-guard, queue-order-guard, bundle-first-guard, prep-guard, retro-currency-guard — Stop chain; the QUALITY_PRESETS unit test) verified wired in .claude/settings.json; topic files stay for the blocked reader, ~230 words" },
    { "id": "U53", "from": ["A48", "B46", "B47", "B48"], "defect": "MEMORY.md CLAUDE.md-duplicate cluster (serving-model-watch, hard-cases-go-to-sol, hybrid-test-architecture, regression-tiers, maximal-delegation, push-after-every-commit, verify-gui-on-both-backends, test-realistic-zoom, journal-voice-markup, update-docs-on-change-requests, lint-and-cve-clean-always, claude-71-reference-not-duplicate, new-tasks-append-and-defer, tasks-spec-final-state-only, doc-budget-shorten-dont-raise, effort-high, audit-with-model-diversity, model-diverse-by-criticality, verify-before-merge, process-scoped-regression, test-coverage-err-on-more, brief-driven-delegation): index lines DELETED as second copies of rules CLAUDE.md already binds (post-cut CLAUDE.md keeps each binding sentence per the stagger rule, so nothing is orphaned), ~400 words" },
    { "id": "U54", "from": ["A49", "B49", "B50"], "defect": "MEMORY.md batch-owner/board-operation cluster (never-stop-the-batch, point-boundary-always-autonomous, batch-autonomy-hardened, hand-over-at-the-watermark, no-other-window-to-close, withdraw-claim-when-leaving, verify-owner-really-dead, always-prep half already in U52, board-cli-is-not-parallel-safe, batch-dashboard-artifact, dashboard-multiple-now-cards, dashboard-no-auto-open, board-transport-not-private, arm-dormant-guards-attended): axis b; MOVED -> an owner-only index inside docs/batch-owner-runbook.md, served by batch-resume-hook.mjs to the lock holder; B49's guard-covered dashboard drops overlap U52 and land there, ~300 words" },
    { "id": "U55", "from": ["A50"], "defect": "MEMORY.md checkpoint-tied entries (release-order-communication-first, queue-order-v02-bugfixes-only, audit-205-decisions, pending-queue-work-29-07): DROPPED-NEEDS-RULING — each is tied to a checkpoint or filing job that may be spent; check whether point 224, the 205->208 filings and the carrier are closed, then ask, ~90 words" },
    { "id": "U56", "from": ["B52"], "defect": "MEMORY.md all surviving entries: B proposes bare [title](topic.md) links; CORRECTED to the file's own rule — ONE line per entry, THE HOOK ONLY (a bare link loses the hook that tells a session when to open the file); detail beyond the hook moves to the topic file, ~300 words (net of the hooks kept)" },
    { "id": "U57", "from": ["A51"], "defect": "MEMORY.md has NO entry in DOC_BUDGETS (scripts/doc-budget-core.mjs budgets only CLAUDE.md, design.md, TASKS.md, acceptance-criteria-detail.md): ADD a MEMORY.md budget plus a per-entry length ceiling so doc-budget-guard (Stop chain) HOLDS the cut instead of letting it regrow — an execution requirement, not a word cut" },
    { "id": "U58", "from": ["A52", "B43"], "defect": "MEMORY.md header block: the review/compaction provenance dates; axis c; MOVED -> docs/rule-corpus-audit.md (which the sentence already names), keeping 'One line per entry: the hook only. Detail belongs in the topic file.', ~40 words" },
    { "id": "U59", "from": ["A53", "B53"], "defect": "global-CLAUDE.md preamble: the cross-project/adaptable-defaults/propose-not-impose caveat; VOID by the user's 20.08.2026 single-project ruling and actively harmful (invites treating the lines as overridable); DROPPED, ~55 words" },
    { "id": "U60", "from": ["A54", "B54", "B55"], "defect": "global-CLAUDE.md Testing & verification: hybrid-layers, reachable-states, retry and root-cause bullets DELETED as less-precise duplicates of CLAUDE.md §5/§7.2; the genuinely additional rules MOVED -> CLAUDE.md §7.2 — B's broader keep wins: BOTH the invariant-assertion channel AND the fail-soft-on-transients/fail-loud-on-product-bugs rule move in (A kept only the former), ~250 words off the global file" },
    { "id": "U61", "from": ["A55", "B56"], "defect": "global-CLAUDE.md Change tracking & commits: all four bullets DELETED — work log/append-and-defer COVERED -> tasks-spec-guard + tasks-archive-guard (Stop), atomic commits by CLAUDE.md §6 + commit-msg hook, clean tree by criterion 18 + pre-push gate, docs-in-lockstep by §4 + rule-echo-guard, ~130 words" },
    { "id": "U62", "from": ["A56", "B57"], "defect": "global-CLAUDE.md Progress visibility: both bullets DELETED — COVERED -> the five dashboard Stop guards + board-first-guard (PreToolUse), and 'never restructure without a go' already lives in the batch-dashboard-artifact memory topic, ~110 words" },
    { "id": "U63", "from": ["A57", "B58"], "defect": "global-CLAUDE.md Quality passes & audits: all three bullets DELETED as second copies (four-eyes in CLAUDE.md §6, phased QA in docs/maximum-qa.md via its memory entry, fan-out budget in the workflows-token-budget memory topic which survives), ~105 words" },
    { "id": "U64", "from": ["A58", "B59"], "defect": "global-CLAUDE.md General conduct: B's broader account wins — all THREE user-level bullets ('act on judgment', 'confirm before outward-facing/hard-to-reverse steps', 'report outcomes faithfully') MOVED -> CLAUDE.md §6 as one line each (closing-guard covers only version tags, not publishing/deleting/force-pushing generally, so the confirm rule must not be dropped as covered); the looks-wrong-but-passes sentence DELETED as a duplicate of the render-verification policy + watch-for-aesthetic-oddities memory, ~95 words" },
    { "id": "U65", "from": ["A59"], "defect": "global-CLAUDE.md as a whole: after U59-U64 only moved lines remain, so the file would be EMPTIED and stop loading; DROPPED-NEEDS-RULING — it is the user's own private file and its deletion is his call, ~750 words gross (~60 of which land in CLAUDE.md)" },
    { "id": "U66", "from": ["A60"], "defect": "Execution constraint for every cut above: each CLAUDE.md cut lands in the SAME commit as its MEMORY.md/global-CLAUDE.md twin (rule-echo-guard blocks a turn end when a moved rule's restatement survives elsewhere), and doc-budget-guard's CLAUDE.md ceiling is LOWERED in that commit so the freed headroom cannot silently refill" }
  ]
}
