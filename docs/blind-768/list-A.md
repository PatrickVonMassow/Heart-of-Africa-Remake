A1 | title block | DROP | agent-facing | "This binding file governs the agentic build: scope, stack, commands, acceptance, and verification" is a table of contents for a nine-section file; no-agent-would — nobody misreads the file for lack of it; cut ~16 words
A2 | §1 | KEEP | agent-facing | "a runnable proof of concept … not the complete game" is the scope anchor every §2 guardrail hangs on; without it an agent builds toward the finished game
A3 | §1 | KEEP | agent-facing | "`design.md` is authoritative for the target state; this file governs how it is built" settles which document wins a conflict, and nothing else in the repository states that precedence
A4 | §2 bullet 1 | KEEP | agent-facing | the user's own test case: without "single-player only: no multiplayer, netcode, roles, or synchronization" an agent starts building multiplayer unasked
A5 | §2 bullet 2 | KEEP | agent-facing | "no onboarding, tutorial, guided introduction, or lowered entry barrier" fails the same test — a helpful agent adds a tutorial by default
A6 | §2 bullet 3 | KEEP | agent-facing | "do not invent or reintroduce systems absent from `design.md`; record a missing design concept as an open item" is the anti-invention rule; nothing enforces it
A7 | §2 bullet 4 | KEEP | agent-facing | the balance rule names the FILE (`src/config/balance.ts`) an estimate must land in; without it estimates scatter through the code, which is exactly what happened before
A8 | §2 bullet 5 | SHORTEN | agent-facing | "Only §7 is in scope … until §7.1 is fully met" re-lists multiplayer and onboarding, already bound by bullets 1 and 2; keep "Only §7 is in scope; begin nothing beyond it until §7.1 is fully met"; cut ~14 words
A9 | §3 bullet 1 | DROP | agent-facing | "Vite; React + TypeScript; three.js; React Three Fiber; drei" is stated more precisely in `package.json`, which is the installed truth and cannot go stale; no-agent-would — an agent in this tree cannot pick a different stack by accident; cut ~10 words
A10 | §3 bullet 2 | KEEP | agent-facing | WebGPU primary with automatic WebGL 2 fallback, TSL rather than raw GLSL/WGSL, no Chrome-only behaviour, the localized dismissible notice — four binding decisions no guard asserts and an agent would break by writing a raw shader
A11 | §3 bullet 3 | DROP | agent-facing | "if WebGPU gets stuck during the run, fall back to plain WebGL and record an open item" contradicts the live rule two bullets above and §7.2 ("WebGPU is the everyday lane; every browser suite asserts the requested backend"); it is U6 of point 763 and leaves with this edit, once; cut ~22 words
A12 | §3 bullet 4 | SHORTEN | agent-facing | the read-aloud bullet's "WebGPU on Chromium and WASM elsewhere; pre-warmed, lazy-loaded" is stated more precisely in `docs/tts-architecture.md`, which the bullet already names; keep "kokoro-js in a Web Worker, absent from startup chunks, English-only"; cut ~14 words
A13 | §3 bullet 5 | KEEP | agent-facing | "add no runtime dependency without necessity and justify each one in its commit" is unguarded and an agent reaches for a package by reflex
A14 | §4 sentence 1 | DROP | agent-facing | "`design.md` points to `docs/design-reference.md` for its extracted reference sections" is navigation, not a rule; `scripts/point-brief-core.mjs` resolves those sections for a delegated author and `design.md` says it itself; cut ~11 words
A15 | §4 sentence 2 | KEEP | agent-facing | "a requested design change updates `design.md`, this file … and the code together" is the one rule that stops the design document drifting from the code; unguarded
A16 | §4 sentence 3 | KEEP | agent-facing | "organize game code by topic under `src/`; do not create a monolith" is unguarded and is broken by default when a file grows
A17 | §5 command block | DROP | agent-facing | the eight-line fenced npm list is stated more precisely in `package.json` `scripts`; the three tier names that carry meaning survive in the sentence below it; cut ~14 words and 10 lines
A18 | §5 tier sentence | KEEP | agent-facing | "choose Vitest-only, SMALL, or LARGE by the changed behavior; the closing always runs LARGE" plus the `scripts/verify/tiers.mjs` pointer is the only statement of the tier choice an author makes
A19 | §5 test-layer paragraph | KEEP | agent-facing | the Vitest/Playwright cut and "every feature adds a test on the right layer" is enforced by nothing and is the most frequently broken rule in the file
A20 | §6 bullet 1 | SHORTEN | agent-facing | "work in small, self-contained commits" belongs in the branch bullet below, which already says "commit atomically"; fold, cut ~6 words
A21 | §6 branch bullet | KEEP | agent-facing | "own `feat/<point>-<slug>` branch from `main`", "commit atomically and push after every commit; report a failed push" — `push-arrival-guard` is in the STOP chain, so it fires after a turn has already ended local-only; the preventive sentence stays
A22 | §6 branch bullet | DROP | agent-facing | "the versioned commit hook owns rescue-commit syntax" is stated again in §7.2 ("the versioned hooks installed by `npm install` own commit and push syntax") AND enforced before the commit exists by `scripts/git-hooks/commit-msg` → `commit-scope-guard.mjs --message`; cut ~8 words
A23 | §6 branch bullet | KEEP | session-only | "merge only when complete and test-green, with the rendered picture checked on both backends for a backend-sensitive change" — `render-verify-guard` is STOP-chain, and the merge is the owner's act
A24 | §6 merge-ends-branch bullet | KEEP | session-only | `branch-hygiene-guard` is STOP-chain, so it names the debris only after the turn that left it; a delegated author never merges, so the rule is the session's
A25 | §6 worktree bullet | DROP | session-only | "small cross-cutting bookkeeping may land on `main`; delegate larger mechanisms to an isolated worktree" is enforced BEFORE the act by `worktree-reminder` in the PreToolUse chain on the `Agent` matcher, and it binds the dispatcher, not the author who is already in a worktree; cut ~25 words
A26 | §6 TASKS.md bullet | KEEP | session-only | `tasks-archive-guard` and `queue-order-guard` are STOP-chain; the rule binds only a session that owns the batch, since a delegated author is told not to touch `TASKS.md`
A27 | §6 land bullet | KEEP | session-only | `land-point.mjs` is the owner's command and the runbook pointer is already one line; nothing here reaches a delegated author
A28 | §6 point-brief bullet | KEEP | agent-facing | "a delegated author runs `node scripts/point-brief.mjs <N>` … and escalates an ambiguous brief instead of guessing" is the single most agent-facing rule in the file and no guard asserts it
A29 | §6 delegated-authors bullet | SHORTEN | agent-facing | "delegated authors work on their own branch, test, commit, push, and do not merge" is agent-facing and stays; "`commission-guard` owns pool capacity; dispatcher-only work is served to the owner from `docs/batch-owner-runbook.md`" is session-only and `commission-guard` is PreToolUse, so it refuses before the spawn; cut ~14 words
A30 | §6 context-fence bullet | KEEP | session-only | it must NOT become a pointer: `context-fence-core.mjs` `CONTEXT_FENCE_MODE_DEFAULT` is `observe` and refuses nothing, so the preventive text is the whole mechanism today; it binds a session that spawns agents, not an author
A31 | §6 model-policy bullet | KEEP | session-only | mostly the dispatcher's routing — but VERBATIM: twelve files carry `rule:model-policy@4f8dd494` against this exact text through `scripts/rule-echo-core.mjs`, so any re-wording makes all twelve stale at once (verified: `node scripts/rule-echo.mjs` reports ok @4f8dd494)
A32 | §6 model-policy bullet, last sentence | KEEP | agent-facing | "every commit names its author model in a `Co-Authored-By` trailer" is the one agent-facing sentence of that paragraph, and it is why the paragraph cannot simply move to the session part of a split
A33 | §6 four-eyes bullet | KEEP | agent-facing | a point whose spec says "four-eyes task" makes its AUTHOR run the divergent stage; the two modes and `scripts/blind-merge.mjs` are stated nowhere else in binding form, and `mechanism-review-guard` is STOP-chain
A34 | §6 language-files bullet | KEEP | agent-facing | "all player-visible text comes from language files … code, identifiers, labels, comments, and filenames are English" is broken by default by any agent that hardcodes a string
A35 | §6 journal-markup bullet | KEEP | agent-facing | the §15 markup rule is unguarded and invisible from the code; the mechanics pointer to `docs/tts-architecture.md` is already one clause
A36 | §6 answer-from-repository bullet | KEEP | session-only | `container-ask-guard` is STOP-chain, so it blocks the answer only at the turn end, and the rule binds a session answering the user — a delegated author answers no user
A37 | §6 settled-judgment bullet | KEEP | agent-facing | "report failures, skips, and verified outcomes faithfully" is the one rule no mechanism in the project can check, which is precisely why it is written
A38 | §6 comments bullet | KEEP | agent-facing | the `// OPEN: …` convention is a product convention an agent cannot infer, and §9 collects what it produces
A39 | §7.1 header sentence | SHORTEN | agent-facing | "each criterion keeps one acceptance condition here … change all affected copies in one commit" becomes the ONE sentence that says the condition and the evidence live under the same number in `docs/acceptance-criteria-detail.md` and `docs/acceptance-evidence.md`; cut ~20 words
A40 | §7.1 criteria 1–32, condition sentences | DROP | agent-facing | every condition is held complete and verbatim under the same number in `docs/acceptance-criteria-detail.md` (measured 579 lines / 5,599 words for 32 criteria), so the always-loaded copy is the second one; cut ~430 words
A41 | §7.1 criteria 1–32, "Detail: … Evidence: …" lines | DROP | agent-facing | 32 pointer lines carry no information the criterion's own NUMBER does not, once the header sentence states the rule once; cut ~220 words and 32 lines
A42 | §7.1 criteria 1–32, number and title | KEEP | agent-facing | `acceptanceCriteriaFrom` in `scripts/point-brief-core.mjs` parses `/^(\d+)\.\s+\*\*(.+?)\*\*/gm` out of §7.1 and every work-order point that writes "§22" is resolved through it; the compact line must keep exactly that shape and the numbering may not move
A43 | §7.2 bullet 1 | KEEP | agent-facing | "run build and lint always, unit tests for every change, and `scripts/audit-check.mjs` when the lockfile changes" is the gate list every author owes; unguarded for an agent that never reaches a Stop chain of its own
A44 | §7.2 bullet 2 | KEEP | agent-facing | "judge visibility by the rendered projection, never an assumed radius" and "every screenshot declares and contains its actual subject" are the two failures the picture checks kept producing; no mechanism can assert them
A45 | §7.2 bullet 3 | KEEP | agent-facing | the WebGPU/LARGE lane split decides which suite an author runs and what a WebGL-2-only red means
A46 | §7.2 bullet 4 | KEEP | agent-facing | "a retry is SUSPECT and covers nothing" is the rule a red run tempts every agent to break, and nothing observes a retry
A47 | §7.2 bullet 5 | KEEP | agent-facing | the dev-mode invariant rule ("fail soft for transients, loud for product defects") shapes code an agent writes and is stated nowhere else
A48 | §7.2 Stop-family bullet | KEEP | session-only | it says so itself and it is right: every rule named there fires at TURN END, so a session can violate it for a whole turn; a delegated author reaches none of the board, batch, work-order or finding duties
A49 | §7.2 pre-action-hooks bullet | SHORTEN | session-only | `board-first-guard` and `closing-guard` are PreToolUse and refuse before the act, so the bullet is correct that they may be pointers; the third clause duplicates A22's commit-hook sentence — keep one; cut ~10 words
A50 | §7.2 guard-preflight bullet | KEEP | session-only | `guard-preflight.mjs` is a command a session must know to run; no guard can require its own preflight
A51 | §7.2 picture-stability bullet | KEEP | agent-facing | "screenshot metrics are not a golden-image shortcut until `picture-stability.mjs` reports STABLE" prevents a false green an agent produces willingly
A52 | §7.2 last bullet | KEEP | agent-facing | "never … report an unfulfilled criterion as fulfilled" is the honesty rule; unguardable by construction
A53 | §9 paragraph 1 | KEEP | session-only | what the closing REPORTS is the contract §7.1 is written against; `closing-guard` gates the tag but does not state the report's content
A54 | §9 paragraph 2 | KEEP | session-only | naming `CLOSING_STEPS` as the authoritative sequence and the two driving flags is the pointer that makes the rest of §9 unnecessary
A55 | §9 paragraph 3 | DROP | session-only | "confirm `docs/graphics-detail-levels.md` still matches `QUALITY_PRESETS`" is ONE closing step among fourteen and is enforced before the act: `closing-guard` (PreToolUse) denies a version tag until `CLOSING_STEPS` entry `graphics-detail-doc` ("docs/graphics-detail-levels.md matches QUALITY_PRESETS") is recorded — verified in `scripts/closing-guard-core.mjs`; cut ~28 words
A56 | §9 paragraph 4 | KEEP | session-only | the closing FREEZE binds in-flight branches and other agents and is enforced by nothing before the fact
A57 | §7 heading | DROP | agent-facing | "## 7. Acceptance" carries a heading and nothing else between it and "### 7.1"; the two sub-headings stand alone; cut 3 lines
A58 | whole file | KEEP | agent-facing | after this cut the SESSION-ONLY remainder is roughly 240 words (A23, A24, A26, A27, A30, A31, A36, A48, A49, A50, A53, A54, A56) out of ~1,180, so a split would buy a delegated agent ~240 words / ~0.3k tokens while costing a second document, a second budget, a heading sweep and a new way for a rule to end up in neither half — the earlier proposal assumed a 61.6-KB file with a ~19-KB agent core, and that ratio no longer exists; DO NOT SPLIT, and say so in the point's report
A59 | mechanism | KEEP | agent-facing | the ratchet: `scripts/doc-budget-core.mjs` refuses growth past a ceiling but a ceiling only falls by hand, so a document measuring more than a stated ABSOLUTE word slack BELOW its ceiling must refuse with "lower the ceiling to what you achieved" — headroom cannot be banked and each document can only ratchet down
A60 | mechanism | KEEP | agent-facing | the second rule: NO PROSE RATIONALE in `CLAUDE.md` — a paragraph that ARGUES rather than INSTRUCTS is a finding in the same shape as the budget; the reason belongs beside the switch it governs, in code or under `docs/`
