{
 "model": "GPT-5.6 Sol",
 "entries": [
  {
   "id": "B1",
   "file": "TASKS.md",
   "defect": "DEFECT: On 17.08.2026 at 19:59 “Woran ich gerade arbeite” was empty and at 20:07 contained only point 700, although `batch-in-flight --status` proved points 697, 700 and 711 were concurrently in flight, pushed and not merged; the nine open `feat/*` branches were not themselves proof of active work. No existing guard covers this: the actual-work check accepts evidence supporting any displayed/focus point and never compares the complete active-point set, while the queue, topic, conciseness and done-claim guards address different invariants."
  },
  {
   "id": "B2",
   "file": "scripts/dashboard-integrity-guard-core.mjs",
   "defect": "FINAL STATE: The numbered membership of “Woran ich gerade arbeite” is a projection of the authoritative structured active-work record, while each card’s German reader-facing title, body and state-specific metadata remain explicitly human-authored; reject the alternatives of wholly hand-maintained membership because it caused the measured omission, and generated prose because point 491 proves that reconstruction or stub generation can destroy authored meaning."
  },
  {
   "id": "B3",
   "file": "scripts/batch-in-flight-core.mjs",
   "defect": "FINAL STATE: Normalize the active-work record as the live batch owner’s numbered focus plus explicitly point-tagged in-flight strands; group branch, worktree, PID and log evidence by an integer point instead of inferring identity from branch names, deduplicate multiple agents/evidence for one point, and treat authoring, counter-read, verification, ready-to-land, landing and valid transferred-awaiting-adoption phases as active."
  },
  {
   "id": "B4",
   "file": "scripts/batch-in-flight-core.mjs",
   "defect": "FINAL STATE: Open or recently moving branches that are absent from the structured record do not create now-cards; an untagged legacy strand, malformed point, closed/unknown TASKS point, contradictory checkpoint or unreadable active record is an unresolved state that must be reported and must never be interpreted as zero active points."
  },
  {
   "id": "B5",
   "file": "scripts/board-core.mjs",
   "defect": "FINAL STATE: The now-section contains exactly one numbered card for every normalized active point and no other numbered card; an active point is removed only by an explicit exit transition—landed/completed to Erledigt, returned/decommissioned/abandoned while still open to its work-order queue position, or blocked solely on the user to “Von dir zu klären”—and a reopened point returns to the queue until work is explicitly recommissioned."
  },
  {
   "id": "B6",
   "file": "scripts/board-core.mjs",
   "defect": "FINAL STATE: Transfer, adoption, session compaction, counter-review and handover are continuity states rather than exits, so their numbered cards remain in “Woran ich gerade arbeite”; pausing the batch also does not silently erase genuinely running work, and evidence becoming stale or ownership becoming ambiguous requires reconciliation rather than automatic card removal."
  },
  {
   "id": "B7",
   "file": "scripts/board-core.mjs",
   "defect": "FINAL STATE: Reconciliation moves existing numbered card markup between sections without replacing its authored title or body, never creates stub copy, and changes prose or section-specific metadata only from explicit operator input; missing copy for a newly active point and conflicting copies for the same point refuse reconciliation without modifying any card, while a deliberate replacement preserves the previous copy until the whole operation succeeds."
  },
  {
   "id": "B8",
   "file": "scripts/board-core.mjs",
   "defect": "FINAL STATE: Surviving cards retain their prior relative order, new cards follow the structured strand/start order with point number as the deterministic tie-break, repeated evidence for a point produces one card, and reconciliation is idempotent and preserves Unicode, links and arbitrary authored inline markup."
  },
  {
   "id": "B9",
   "file": "scripts/board-core.mjs",
   "defect": "FINAL STATE: A verified zero-point active record renders no numbered now-card but does render a dedicated non-card German empty-state message meaning “there is currently no demonstrably active point”; missing, malformed or unverifiable source data never renders that message, never clears the section and prevents publication while preserving the last authored board."
  },
  {
   "id": "B10",
   "file": "scripts/board-publish.mjs",
   "defect": "FINAL STATE: Publication performs the exact-set check against the normalized record and refuses a missing, extra, duplicate, unknown or cross-section point; active-set lifecycle commands reconcile and publish through one serialized board operation, never concurrent `board.mjs` calls, and a failed reconciliation/publication remains a named non-zero pending obligation rather than silently advertising success."
  },
  {
   "id": "B11",
   "file": "scripts/dashboard-integrity-guard.mjs",
   "defect": "FINAL STATE: The owner’s Stop-hook uses the same pure comparison and blocks a readable mismatch with missing and extra point numbers plus the serial reconcile/publish remedy; consistently with all existing Stop guards it stands down for `heldByOtherLiveOwner` and `.claude/batch-paused` and fails open on its own I/O/error path, whereas the explicit publish preflight may fail closed because no publication is safer than publishing a knowingly false board."
  },
  {
   "id": "B12",
   "file": "scripts/board-core.mjs",
   "defect": "FINAL STATE: Point 700’s undecided handover-card clause is resolved by never substituting an unnumbered handover card for active work: an in-flight handover retains the same numbered point card, and the verified-zero state uses a parser-distinct empty-state element rather than a card."
  },
  {
   "id": "B13",
   "file": "scripts/dashboard-integrity-guard-core.test.mjs",
   "defect": "VERIFIABLE: With expected active points `[697,700,711]`, an empty now-section returns `missing:[697,700,711]` and blocks; a sole point-700 card returns `missing:[697,711]` and blocks; exactly the three distinct cards allows; and any fourth, stale card returns it in `extra` and blocks."
  },
  {
   "id": "B14",
   "file": "scripts/dashboard-integrity-guard-core.test.mjs",
   "defect": "VERIFIABLE: Two now-cards for one point report a duplicate even if set equality holds; one point in now and queue/clarification/archive reports the existing cross-section violation; a nonempty active record is still checked when every board section is otherwise empty; and the former “no board—registration owns it” escape cannot suppress a known nonempty active-set mismatch."
  },
  {
   "id": "B15",
   "file": "scripts/batch-in-flight-core.test.mjs",
   "defect": "VERIFIABLE: Multiple branch/worktree/PID items tagged to one point normalize to one point; focus 700 plus strands 697 and 711 normalize to all three; nine undeclared `feat/*` branches add none; pushed-unmerged review/landing phases remain active; terminal exit phases do not; and untagged, malformed, unknown, closed or checkpoint-contradictory records return `unknown/error`, never an empty active set."
  },
  {
   "id": "B16",
   "file": "scripts/board-core.test.mjs",
   "defect": "VERIFIABLE: Promotion, return to queue order, move to user clarification, answered-decision return, archive, abandonment, reopen and recommission each leave the point in exactly one correct section; transfer and adoption retain the numbered now-card; and a verified no-active-points scenario yields only the dedicated empty-state element."
  },
  {
   "id": "B17",
   "file": "scripts/board-core.test.mjs",
   "defect": "VERIFIABLE: Reconcile preserves authored German title/body, Unicode, links and metadata byte-for-byte for unchanged fields; no prose becomes a stub; explicit replacement changes only the supplied field; missing copy or conflicting duplicate copy fails without partial mutation; stable ordering and a second identical reconciliation produce byte-identical HTML."
  },
  {
   "id": "B18",
   "file": "scripts/dashboard-integrity-guard-core.test.mjs",
   "defect": "VERIFIABLE: Malformed/unreadable source data is total and fail-open in the Stop decision but fail-closed in publish preflight; paused and other-live-owner Stop cases allow without changing board state; exact equality allows; mismatch diagnostics name missing/extra/duplicate points; and the composed test retains existing single-topic, no-false-done, queue-order and now-ETA enforcement for every projected card."
  },
  {
   "id": "B19",
   "file": "scripts/board-core.test.mjs",
   "defect": "VERIFIABLE: The point-700 “promotes, returns, archives and answers without a new violation” test starts continuity/handover cases with a numbered active card, never an unnumbered handover card, and separately proves that the parser-distinct verified-zero empty state needs no fictitious card to move."
  },
  {
   "id": "B20",
   "file": "TASKS.md",
   "defect": "Criticality: CRITICAL—this is a published user-facing control surface for autonomous work, its measured state omitted two of three active points, and no current invariant can detect the omission."
  },
  {
   "id": "B21",
   "file": "TASKS.md",
   "defect": "Bundle: Land with point 700’s board-core handover/lifecycle work and the dashboard-integrity plus batch-in-flight record changes, because separating them would preserve either the duplicate-card gate failure or the incomplete active-set source."
  }
 ]
}
