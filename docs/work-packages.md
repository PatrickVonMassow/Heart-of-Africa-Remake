# Work packages (bundles)

**ONE BRANCH PER POINT, not per bundle** (user decision 30.07.2026). The original
rule read "a bundle is ONE branch, ONE verification and ONE regression round".
The user weighed it and decided against, on two grounds that hold: the regression
is already SCOPED per change — a scripts- or docs-only point runs the Vitest layer
and nothing else, so the "saved round" it would buy is two minutes — and a whole
bundle on one branch lands every one of its features in a single merge, which is
neither reviewable nor attributable when one of them is wrong.

**THE BUNDLE SAVES NEITHER TIME NOR TOKENS — say so, do not re-derive it.** The
scheme was cut on 29.07.2026 with a saving as its stated purpose ("n points cost
one verification round together and n apart"), and by 30.07.2026 the user had
taken that claim apart, step by step, and it does not survive:

- The regression is SCOPED per change, so a scripts- or docs-only member's
  "shared round" is a two-minute Vitest run.
- Nothing is carried between two points of a bundle: each goes to a
  worktree-isolated agent with a fresh context and a brief, and the main session
  hands over at every point boundary. The "related code stays fresh" line that
  stood here was simply wrong.
- The one real carry — handing the next point to the agent that already has the
  files open, as with 439 → 452 — is possible only on FILE OVERLAP, and it is
  fenced in by point 471 (full brief closing the previous point, one commit per
  point, no third point in one context, dropped if reused work draws more review
  findings). What is left of the saving is small and deliberately capped.

So the grouping is kept for what it actually does, and the savings argument is
retired:

- **The ORDER** is the priority ranking. Nothing more.
- **The COLLISION MAP.** The split follows SHARED FILES, so it says which points
  must NOT run in parallel. Two points in the same bundle that touch the same
  module go on ONE branch — one commit each — because parallel agents would
  otherwise overwrite each other. That is the only case where a branch carries
  more than one point.
- **NOT the board.** The queue was grouped by bundle on 30.07.2026 and the user had
  it taken back out the same evening (point 472): a flat queue IS the working order,
  read top to bottom, while a grouped one is not — the pool draws its three slots
  from different groups. The grouping cost clarity instead of adding it. The bundle
  is never rendered.

The collision map is the only one of the two that is load-bearing, and it is a
HAND-MAINTAINED APPROXIMATION of something measurable: which points touch the same
files. Deriving it instead of curating it would make the grouping both cheaper and
harder to get wrong — an open thread, not a decision taken.

Where the heavy verification really is per-branch expensive — the render bundles,
whose points need the browser suites on both backends — the saving is taken at the
END: several finished per-point branches merge, and ONE regression runs over the
merged result. That saving is real and is the only sizeable one left, but it comes
from BATCHING THE MERGE, not from the bundle — any set of finished branches can be
merged together, related or not.

AND UNTIL POINT 471 LANDS THE SCHEME COSTS TIME. The order walks the bundles
strictly in sequence while a bundle's members are, by construction, the points that
cannot run beside each other — so the leading bundle can feed one agent while two
of three slots stand idle. That is not a small residual; it is the largest single
effect the bundling currently has on wall-clock, and its sign is negative.

Bundles A–J were agreed with the user on 29.07.2026. K, L and M were cut the same
evening for the open points the original scheme never covered, under the user's
standing authority over the bundling ("Mache die Bündelung und Reihenfolge so, wie
du sie für gut hältst"). The scheme had drifted within an hour of being written —
it covered 53 of 91 open points, listed one already-closed point, and nothing
compared it against the work order. Hence the property to preserve:

> **Every open point in `TASKS.md` appears in exactly one bundle here, or in the
> unbundled list below.** A new point joins a bundle when it is appended.

**A reference is written `#123`.** That marker is what places a point — in a
bundle's Points cell and in a "Not bundled" bullet alike. Everything else in the
cell is prose, and the reader does not look at it: a date, a measurement, a
number standing in front of a month name, a point named only in passing. So a
new member is written into the cell WITH its marker; naming it in the prose
alone leaves it unplaced, and the guard says so. Until 28.08.2026 the reader
took every number in the cell and subtracted known date shapes first; four
cross-vendor rounds refused that in turn, because each filter closed one shape
and earned the next — no regular expression over prose can tell a reference from
a measurement (point 1003).

**Every bundle is SPOKEN by its name, never by its letter** (user 30.07.2026: "Die
Buchstaben sagen nichts aus"). The name is what goes into a chat answer, a board
card and a point text; the letter survives only as this table's internal id, so the
point texts written before the naming stay valid. A newly cut bundle gets its name
in the same moment — a letter alone is not a complete definition. The German name is
the one the user reads (memory `bundle-names`, retrospective §3.66).

## The bundles

| Name | Id | What it is | Points |
|---|---|---|---|
| **Dorfleben** | A | Village life | #648, #350, #351, #356, #357, #359, #360, #394, #578, #653, #680, #681, #682, #686, #687, #688, #689, #690, #691, #692, #694, #698, #1045, #1046, #1047 (648 first — the user is blocked on it; 1047 is the user's eight rulings on the bank round he watched live on 02.09.2026, worked at the front of the order by his decision — it edits `bankGame.ts` and the play-rock geometry that 1045 and 1046 also reason about, so it is not worked beside them) |
| **Wetter & Wasser** | B | Weather, ground and water surface | #314, #320, #321, #323, #348, #353, #354, #358, #384, #385, #500, #501, #522, #523, #548, #603 |
| **Siedlungsgeometrie** | C | Settlement geometry | #299, #349, #352, #380, #415, #428, #581, #583, #604, #611 |
| **Sonne & Himmel** | D | Sun and sky | #343, #344, #345, #346 |
| **Monumente** | E | Monument sites | #315, #379, #391 |
| **Tierverhalten** | F | Animal behaviour | #265, #269, #312, #362, #363, #364, #414, #565, #575, #725, #1013 (1013 is the cheetah scored as hunting in central Africa, where the food web does not carry it — one mismatch in sixteen hunts, and it is unmeasured whether the check reads the region of the KILL rather than of the start; it edits the predator placement and the `predator-food-web` section of `scripts/verify/enrichments.mjs`, which no other open point of this bundle touches, so it may run beside any of them) |
| **Kadaver & Geier** | G | Carrion, vultures, staging | #319, #322, #326, #327, #328, #336, #453 |
| **Chat & Tafel** | H | Chat and board | #1020 (1020 is the handover card judged for length only AFTER the boundary fence has forbidden changing it — measured 31.08.2026 over six turns that moved nothing, because the prescribed remedy is a republish and every post-commit mutation is refused, rightly; it edits the board's card gate and the boundary's card wording, so it is not worked beside another point touching the board publish path, and it stands behind the release because it costs no correctness), #440, #451, #465, #467, #473, #539, #491, #508, #520, #621, #625, #664, #665, #699, #704, #705, #706, #713, #720, #728, #730, #731, #740, #749, #777, #793, #799, #800, #828, #838, #843, #844, #869, #884, #885, #899, #930, #937, #940, #952, #954, #959, #965, #976, #978, #989 (899 is the board-first exemption that never fires for a delegated author — the guard reads the hook process's own checkout instead of the caller's, so the worktree carve-out misses exactly the sessions it exists for and the refusal points at a board card the author cannot write; it edits the board-first guard and its core that 869 and 843 also reach through the session selection, so it is not worked beside them, and it stands behind the release beside the other guard defects of the same days, because the measured block cleared itself and cost no work; 885 is the chat message a delegated agent swallowed — a subagent runs the owner's own heartbeat hook under the owner's session id, claims the pending message, and its hook output is never injected anywhere, so the user's question vanished and the defer-sweep could not see it because the claim had already emptied the pending set; it edits the delivery path 793 and 799 also own, so it is not worked beside them, and it stands BEFORE the release because every delegated round reopens the window; 884 is the pair of dated fuses — the card-topic guard reads point numbers only to 999 and goes blind at 1000, and the guide suite claims both budgets while measuring one; 869 is the board command that the session told to run it cannot run — the Stop hook prescribes it for every open decision card, but it identifies its session through CLAUDE_SESSION_ID, which a chat-responder session does not carry, and board.mjs has no --session flag, so the only way through was manual archaeology in the guard state; it edits the board CLI and the decision-card gate's session selection that 843 also owns, so it is not worked beside it, and it stands behind the release because the machine filed it; 843 is the register of the owner's settled rulings — he reported on 22.08.2026 that a card asked him to confirm a raise he had already delegated — and it edits the decision-card gate and the reply guard this bundle owns, so it is not worked beside anything else on that path; 844 is the criticality badge he asked for in every card header, which edits the same card-summary path 838 rewrites, so the two are not worked beside each other; 838 is the pair of board estimates that contradict each other — a front stage priced above the whole it was cut out of, measured against 106 landed points — and it edits the board cards and their estimates this bundle owns, so it is worked after anything else that rewrites the same cards; 793 is the deadlock between the pause and the chat watcher, found 20.08.2026 when the user's two answers sat unread for 19 minutes — it edits the watcher's wake decision, so it is not worked beside anything else that touches the chat path; 799 is the residue of that same change — a supervision answer that can no longer be returned, still promised in the comment and still handled by the launcher — so it edits the path 793 just touched and is read straight after it; 800 is the contradiction between the boundary's dictated handover card and the board's publish gate, which edits the board publish path this bundle owns, so it is not worked beside 787, which filed the same defect from the other side; 828 is the chat reply that published the name of its own flag instead of the answer, reported by the user on 22.08.2026 — it edits the same outward prose path this bundle owns and is the one point of it that stands before the release), 902 (902 is the standing authorization that a board card carrying my own stated recommendation may be decided by it, ruled by the user on 24.08.2026 — it edits the `CLAUDE.md` §6 rule text and `docs/rule-corpus-audit.md`, which no other open point of this bundle touches, so it may run beside any of them), 935 (935 is the block point 713 measured on itself twenty minutes after landing: with nothing running, the module's OWN derived state card made the now-section unpublishable, because the exemption the eighth round gave that card existed only in the branch beside active work. It edits the very board render and fail-closed preflight 930 also reaches, so the two are never worked beside each other — 935 goes FIRST, because until it lands no session can publish its handover at all), 930 (930 is the one disagreement the ninth cross-vendor round on point 713 could not settle inside it — the section removes a card whose point has left the flight, and it may never blank text a session wrote; it edits the very board render 713 rewrites, so it is worked AFTER 713 has landed and never beside it, and it stands behind the release because the loss needs a path the ordinary lifecycle does not take), 936 (936 is the decision log that has no end but the user's veto — the alert ladder's own escalation record renders a board card for as long as the record stands, and nothing ever removes the record, so a decision whose risk the 08:32 doctor run of 26.08.2026 refuted keeps reading as an open matter; it edits the board's derived state card, the same render 930 and 937 reach, so it is not worked beside them), 937 (937 is the closing state the derived now-section made unreachable — the board gate names a closing card as the way out between the merge and the cleanup, and the projection removes that card in the same operation because it is numbered and its point is no longer open, which is how the last landing's finished branch stayed alive; it edits the same projection and the point-470 gate that 930 and 936 reach, so it is not worked beside them), 940 (940 is the pair of findings the cross-vendor review returned AFTER point 935 had landed — the regex that grants a card derived identity accepts shapes the writer never produces, so a hand-written card can be exempted and then silently deleted, and the suite cannot catch it because it has no marked, unnumbered, non-writer-shaped case; it edits the same board projection 930, 936 and 937 reach, so it is not worked beside them), 941 (941 is the card header the user reported on 26.08.2026: only the middle column wraps, so on a narrow phone the number-and-badge group and the times group squeeze the title or push the row past the card. He asked for it NEXT, so it stands at the head of the order; it edits the card summary markup and its styles, which no other open point of this bundle touches today), 946 (946 is the VDZK admissibility typing the user's 26.08.2026 no-standstill order demands — an owner-decidable question is admissible only as the record of a decision already taken; it edits the decision-card gate 843 and 902 also reach, so it is worked after them, never beside them) — the rest landed 30.07.2026 (308, 410, 411, 416, 421, 423, 424, 430, 435, 436, 441, 439, 452, 472, 470), 952 (952 is the board-layout suite that renders a hand-written COPY of the board stylesheet instead of reading the base rules from the source the board writes them from, so it would stay green after its own subject moved; it edits the board suite and the stylesheet source that 844 and 838 reach through the card header, so it is not worked beside them), 954 (954 is the card header that still does not really break on a phone: the wrap delivered by 941 holds its stated acceptance — nothing overflows on the live board — but the right group carries `flex-wrap: nowrap` and the left group is never squeezed below its one-line width, so only the title reflows while both outer groups keep their full width. It edits the very card header 844 and 838 own and is measured by the suite 952 fixes, so it is worked AFTER 952 and never beside it), 959 (959 is the done card reporting only its LAST run, so a point worked over several sessions understates what it cost — the user ruled on 27.08.2026 that a finished point shows its accumulated in-work duration instead of a start·end pair; it edits the card transitions in `board-core.mjs` that 930, 936 and 937 also reach, so it is not worked beside them), 963 (963 is the user's THIRD report of the same card header: the summary and the title were taught to wrap, the two columns beside them were not, so a narrow phone still scrolls sideways; it edits the one dashboard structure the other cards of this bundle also render through, so it is worked alone rather than beside them), 965 (965 is the closing card the board's own refusal prescribes and the derived now-section then discards, so the handover of every landed point can only pass the guard by claiming work that is not running — it edits that derivation, which 963's header layout does not touch, so the two may run beside each other), 967 (967 is the fourth round of the rejected portrait card header — the user's approval closes it, recorded 27.08.2026, and it stands at the queue head by his order; it edits the board card-header CSS and the portrait layout suite that 844 and 838 also reach through the card summary, so it is not worked beside them), 969 (969 is the user's fourth report of the same portrait card header — 941, 963 and 967 before it — filed 27.08.2026 after 967 ticked done without the user approval its own spec demanded; it closes only on the user's approval of a portrait candidate screenshot, it edits the same board card-header CSS and portrait layout suite 967 owns, so it is worked alone on that path, and it stands at the queue head by the user's report of 27.08.2026, 13:33), 976 (976 is the landing that closes every route to the point's own Erledigt card — tick and publish remove the queue and now cards the board commands need, so the only exit left is the emergency waiver, measured 27.08.2026 landing point 971; it edits `scripts/land-point.mjs` and the board card writer that 930, 936 and 937 also reach, so it is not worked beside them), 978 (978 is the done card whose empty text publishes the internal focus note, measured at the landing of 843; it edits the same card writer 976 owns for the missing card, so the two are worked TOGETHER on one branch with 976 first, never beside each other, and it stands behind the release because the board stays readable without it), 989 (989 is the estimate of a current-work card that no board command can write — the audit demands a fresh `~HH:MM` and the only route is queue then promote, two focus transitions and a window with no current work at all, measured 28.08.2026 while working point 734; it edits the board CLI beside the `title` command that already writes both halves, which 976 and 978 reach through the card writer, so it is not worked beside them, and it stands behind the release because it costs turns and hides no defect), #1024 (1024 is the handover card that claims a standstill while a delegated author keeps building — the boundary detects the transferable in-flight work and says so in its own first line, then dictates a card ending in "Hier läuft nichts weiter"; measured 31.08.2026 with and without a declared checkpoint, the closing sentence was identical, so the detection never reaches the text. It edits the boundary's card wording and the board's contradiction check, the same path 1020 and 800 own, so it is not worked beside them, and it stands behind the release because the run continues correctly and only the report of it is wrong), #1049 (1049 is the front-ordered bank-round point found standing LAST of 360 open points on 03.09.2026 — a user front-order and the communication-first release order both live only in prose plus a one-time advisory, so nothing refused the filing turn; it edits the queue-order reader on the board publish path, so it is not worked beside another point touching that path) |
| **Session- & Repo-Hygiene** | I | Session, pool and repo hygiene | #1039 (1039 is the user-ordered redirection of 01.09.2026 — freeze, intake rule, §7.1 as checklist; it edits CLAUDE.md and creates docs/backlog.md, which no other open point of this bundle writes, so it may run beside any of them and is largely executed already), #1042 (1042 is the context handover that cannot be recorded while a FOREIGN session holds a now-card — the refusal reads the whole section instead of this session's own cards, so the watermark boundary could not be written at all. It edits `scripts/batch-boundary.mjs` and the board's none-card refusal, which no other open point of this bundle writes, so it may run beside any of them), #1041 (1041 is the Fable lane nailed to one version — `FABLE_MODEL_ID` reads `claude-fable-5`, nothing holds it against the version actually released, so Fable 5.1 would never have reached us; it moves that constant and the CLI the new id requires. It edits `scripts/fable-switch-core.mjs`, which no other open point of this bundle writes — 736 only quotes the same literal in prose while deciding that the fallback stays inside Anthropic, so the two are not worked beside each other lest one restate what the other moved — and it may run beside the rest), #933, #1000, #934, #929, #931, #932, #925, #924, #922, #920, #912, #911, #910, #909, #908, #906, #904, #898, #901, #463, #471, #553, #554, #646, #660, #683, #696, #708, #710, #724, #733, #738, #739, #741, #746, #747, #750, #754, #755, #756, #759, #762, #763, #764, #765, #766, #767, #774, #785, #786, #791, #797, #798, #802, #803, #804, #805, #806, #807, #810, #814, #815, #817, #818, #819, #822, #823, #824, #825, #826, #827, #830, #831, #832, #833, #836, #837, #839, #841, #842, #845, #847, #849, #850, #851, #852, #853, #855, #856, #871, #878, #881, #882, #886, #939, #960, #961, #972, #974, #975, #981, #990, #991, #992, #994, #995, #996, #997, #998, #1003, #1004, #1005, #1006, #1007, #1008, #1009, #1010, #1011, #1012, #1014, #1015 (933 is the convention that a commit recording a verdict names WHAT it reviewed rather than a pass index the split recuts on every run; it edits `scripts/review-sol.mjs`'s printed record command and `scripts/verify/README.md`, which no other open point of this bundle touches, so it may run beside any of them), 934 (934 is the dependency mechanism cut down to the core the other house's DO-NOT-BUILD verdict prescribes — satisfaction semantics, an activation carrier, lifecycle validation across the archive; it edits the work-order lifecycle tooling that 929 and the fold/split path also reach, so it is not worked beside them, and it owes a repo-backed re-verification before any code), 931, 932, 929, 925, 924, 921, 922, 920, 913, 912, 911, 910, 909, 908, 907, 906, 904, 897, 898, 901, 401, 434, 461, 462, 463, 471, 553, 554, 556, 629, 646, 649, 614, 660, 918, 676, 834, 889, 890, 891, 892, 893, 894, 895, 835, 683, 696, 708, 710, 724, 733, 734, 738, 739, 741, 742, 743, 744, 745, 746, 747, 748, 750, 751, 752, 753, 754, 755, 756, 757, 758, 759, 761, 762, 763, 764, 765, 766, 767, 768, 769, 770, 771, 772, 773, 774, 775, 779, 780, 781, 782, 783, 784, 785, 786, 789, 790, 791, 795, 796, 797, 798, 801, 802, 803, 804, 805, 806, 807, 809, 810, 811, 812, 813, 814, 815, 816, 817, 818, 819, 820, 821, 822, 823, 824, 825, 826, 827, 830, 831, 832, 833, 836, 837, 839, 840, 841, 842, 845, 846, 847, 848, 849, 850, 851, 852, 853, 854, 855, 856, 860, 870, 871, 874, 878, 879, 881, 882, 886 (913 is the takeover drill’s negative control reporting an intermittent THIRD failure — an accepted stale checkpoint’s push lands after the cancellation was read, and the branch-preservation check charges the cancellation with a move it did not cause; it edits `scripts/batch-daemon-drill.mjs` and its test, which 834 and its 889-895 seams also reach, so it is not worked beside them, and it stands at the FRONT of the queue because it reddens `npm run test:unit` — the gate every landing runs — and already stopped the landing of point 907; 889-895 are the seams point 834 was CUT into on 24.08.2026 — the four-eyes ledger repair and the fold, the architecture document, the schema and flag core, the state store, the attempt leases, the daemon and worker contract, and the adoption/reconciliation slice — and they are a STRICTLY ORDERED chain rather than parallel work: 890 first because the artefacts test of 889 binds the counted union to that document's table, then 889, then 891 which every later slice imports, then 892 and 893, then 894, then 895, and 834 last with the drills and the switch. None of them may run beside another, and 874 edits `scripts/batch-in-flight.mjs`, which 895 changes, so it waits for 895 rather than for 834; 874 is the liveness probe that reports a dead authoring run as alive — it never looks at the pid its own declaration records, and it lets a fresh branch tip outrank the process check, which is exactly what a just-died run leaves behind; it edits `scripts/batch-in-flight.mjs` and its core, the same file 834 changes by 46 lines, so it is worked directly AFTER 834 and never beside it; 871 is the boundary card that claims nothing is running while it transfers a live authoring run — measured 24.08.2026 at the point-870 boundary, where the dictated text appends "Hier laeuft nichts weiter." unconditionally; it edits the same handover-card composer 800 corrected from the publish-gate side, so it is not worked beside anything else on that path, and it stands behind the release because the machine filed it; 870 is the review round that cannot be commissioned at all — a merge resolved conflicts across 41 files without an author trailer, so seven end-state files have no provable independent reviewer, and the planner then refuses the WHOLE round rather than the affected files, leaving 87 % readable material unread with no operator route out; it edits the review planner and the unavailable-reviewer receipt that 783, 784, 804 and 856 also own, so it is not worked beside them, and it stands BEFORE the release because it deadlocks the queue itself — point 834 is finished and green and cannot be reviewed or landed until it is fixed; 860 is the alert ladder's pause rung under the 23.08.2026 no-standstill order — a pause verdict survives only for corruption-class conditions, every other ladder end continues and files a retroactive-veto decision card; it edits the alert-escalation core that 434 built and 749 also reads, so it is not worked beside 749, and it is ranked directly behind 859 by the same user order; 856 is the review planner reading a reviewer-credit trailer as authorship, so a commit carrying the pair §6 now permits has no eligible reviewer and its four-eyes debt can only be hand-recorded — it edits the review-planner core that 783, 784 and 804 also own, so it is not worked beside them, and it stands behind the release because the machine filed it; 855 is the pair of test gaps GPT-5.6 Sol's confirming pass on af2ef375 left owed — it edits only the doc-budget and queue-calibration tests that 762, 802, 832 and 845 also own, so it is worked with them rather than beside them, and it stands behind the release because the machine filed it; 854 is the tripwire that reads a commit's two co-author trailers as one unknown model and raises a forbidden-model breach on an allowed pair, measured 23.08.2026 while it blocked this very batch; it edits the model-guard core and the §6 trailer rule that no other open point owns, so it is worked whenever the bundle has room, and it stands behind the release because the machine filed it — the block it causes is cleared by a user confirmation, not by working it earlier; 853 is the guard that judges one question from two sources — the preflight gather reads a live parallel probe while the guard's own run path reads the handled-alert record, so the preflight blocks where the guard allows and advises a full doctor gate that cannot clear it, measured 23.08.2026; it edits the very liveness reading 849 owns, so it is worked AFTER 849 and never beside it, and it stands behind the release because the machine filed it; 852 is the landing gate that cannot run while another author lane commits — the integrity check reads a foreign lane's checkpoint commit as repository damage and reddens a gate in which no test failed, measured 23.08.2026 half-way through a landing; it edits the repository-integrity check and the in-flight declaration that would supply its expected refs, which 801, 805 and 848 also touch, so it is not worked beside them, and it stands behind the release because the machine filed it; 851 is the re-run that cancels the run it was meant to rescue — the repair advice names a stale run while the branch tip's run is in progress, and the shared concurrency group kills the tip, measured 23.08.2026; it edits the CI failure-cause reporter that 803 and 836 also read, so it is not worked beside them, and it stands behind the release because the machine filed it; 850 is the generated command index publishing commands nobody can run — the harvester lifts each usage line out of the source without evaluating it, so six template placeholders across four names are printed verbatim, and the byte-for-byte test cannot see it because both sides carry the same placeholder, measured 23.08.2026 while reviewing 846; it edits the help harvester and its test, which no other open point owns, so it is worked whenever the bundle has room, and it stands behind the release because the machine filed it; 849 is the parallel-session alert firing on the retired predecessor that already handed the batch over — a live pid belonging to the interactive session plus a write stamped at the handover itself is read as a competing writer, so every turn pays a three-minute doctor gate that returns inconclusive under load, measured 23.08.2026; it edits the very singleton classifier 823, 841, 434, 556 and 795 also own, so it is worked WITH 823 and 841 and never beside them, and it stands behind the release because the machine filed it; 848 is the board frozen for hours while a single-turn session works — every currency guard bites only at a turn end, so a session that never ends its turn publishes nothing, measured 23.08.2026 across a fifteen-round review loop; it edits the board-currency and in-flight paths 434, 713 and 787 also touch, so it is not worked beside them, and it stands behind the release because the machine filed it; 847 is the brevity guard measuring less than its own sentence — four paths past the ceilings of the beginner guide, found by one cross-vendor reading; it edits the very guide-brevity core 762, 802 and 832 also own, so it is worked with them rather than beside them, and it stands behind the release because the machine filed it; 846 is the lane the router can choose but no command can serve — point 834 escalates to Fable after 18 unsuccessful review rounds and there is no author-fable command, while the one commission recorder writes the Sol model unconditionally; it edits the authoring-routing and commission-ledger path 753, 780, 796 and 834 also own, so it is worked BEFORE 834 and never beside it; 845 is the unit case that pins the measured SIZE of the user's memory file, which lives outside the working tree and any session may write — so an unrelated point's suite turns red because someone wrote down something worth remembering, measured 22.08.2026; it edits the doc-budget core and the cut account that 762, 802 and 832 also own, so it is worked with them rather than beside them, and it stands behind the release because the machine filed it; 842 is the link witness point 517 built and the three routes it still has to a clean pass over a real link failure — comment stripping that is line-based, a clause matcher that mis-reads rather than refuses, and a package specifier resolved from the checker instead of the subject; it edits only that module and its tests, so it collides with no other open point and is worked whenever the bundle has room; 841 is the same detector's OTHER false positive — the interactive chat session the singleton itself told to stand down, alive and making real calls, counted as a second batch worker and paused the whole batch; 823's fix does not reach it, because that one drops a session whose process is gone while this one's is alive, so it edits the singleton classifier 823, 434, 556 and 795 also own and is worked WITH 823 and never beside it, and it stands behind the release because the machine filed it; 840 is the four-eyes rule losing its grip on the one fact it turns on — a blind half named an author that had not written it, and every command that asks takes the answer from a `model` field the reviewed party writes; it edits the very merge and recorder path 834 depends on and reads the review ledger 837 and 839 wait on, so it is worked WITH 834 and never beside 837 or 839; 839 is the same gate from its other end — the backlog it now refuses on, made of unanswered DO-NOT-MERGE verdicts and contributions whose authoring vendor cannot be resolved at all; it reads the very review ledger and guard 837 waits on, so it is worked WITH 837 and never beside it, and it stands behind the release because the machine filed it; 837 is the four-eyes gate that has been standing open on a SUSPENDED demand rather than a satisfied one — 28 commits stand behind it, four of them from the landing of the output budget this bundle owns, so it is read together with that history and never beside a point that adds to it; 836 is the delegated run whose gate line reads as red because our suite permanently reports five skipped tests, which edits the authoring wrapper this bundle owns and therefore waits on nothing else; 833 is the timestamp guard’s own test losing a minute race under load — it spawns the real guard, which stamps the minute it runs in, then compares against a second stamp taken after the subprocess returned, so a rollover reds it at correct behaviour, measured 22.08.2026 inside the main push gate; it edits only that guard’s test file, so it collides with no other open point, and it stands behind the release because the machine filed it from its own gate run; 832 is the ratchet measured against today's ceiling instead of the one it replaced, plus three heuristic readers of the same guard that give both wrong answers — filed from the cross-vendor review of 768; it edits the doc-budget core 762 and 802 also own, so it is worked with them rather than beside them; 831 is the launcher singleton answered from a file — a live daemon the record does not name is invisible to `--status` and `--start` puts a second one beside it, measured by hand on 22.08.2026; it edits the launcher and the singleton probe that 795, 811 and 823 also own, so it is not worked beside them; 830 is the rule fingerprint that is wider than the rule — `rule:model-policy` runs six bullets past the model policy because §6 has no blank line between its bullets, so editing a neighbouring rule stales twelve stamped files and point 768's counted merge already bent to it; it edits the rule-echo registry this bundle owns and the very §6 that 768 cut, so it is worked after that landing rather than beside it; 827 is the firewall drift measured 22.08.2026 — the container boots an image copy of the script whose repository twin is never executed, so it edits the devcontainer boot path and the additive top-up this bundle already owns, and it stands behind the release because the machine filed it out of a drained finding; 826 is the anchoring half of the same gate — the coverage 820 introduced is measured against the reviewed sha instead of the head being ticked, so a file added after the last review is never even asked about; it edits the very criticality-review core 820 rewrote and 825 rewrites next, so it is worked WITH 825 and never beside it; 825 is the same gate from the other side — its refusal text offers a findings-filed receipt that no command writes, so it edits the very criticality-review core 820 rewrites and is worked WITH 820, never beside it; 824 is the killed authoring run of 21.08.2026 whose uncommitted remains nothing drains and whose declaration keeps reporting it alive off a surviving wrapper process — it edits the in-flight declaration path 813 and 814 own and the authoring-start path 753, 780 and 796 touch, so it is not worked beside those; 823 is the parallel-session detector counting the predecessor that has just handed over, so a phantom pauses the whole batch — it edits the singleton classifier that 434, 556 and 795 also own and reads the launcher start decision 811 touches, so it is not worked beside them; 822 is the board defect of the same evening — a now-card that names only its mechanism and outlives the tick of its own point — so it edits the board card composer and the landing sequence that 434, 787 and 814 also own, and is not worked beside them; 821 is the main-write fence judging the session instead of the resolved target path, so an unlocked session cannot write its own scratchpad — it edits the same fence path 795 and 813 touch and is worked before anything that relies on a stand-down session recording work; 820 is the gate defect measured while landing 769 — the criticality gate rejects the scoped 1/1 review record `review-sol` normally produces, so every HIGH point blocks at its session's turn exit — so it edits the criticality-review core this bundle already owns and reads the review planner 783, 784 and 804 touch, and it is not worked beside those; the one-line floor fix was already implemented, reviewed and refused, and that refusal is written into its text; 819 is the invariant left over after the front-block clearance the user ordered on 21.08.2026 — it edits the very ranking core 789 built and the work order both of them write, so it is worked after 789's rule rather than beside it; 818 is the seven-defect answer of the same commissioned reading — the standstill report blanking or mislabelling the time it measures — so it edits the classifier and its inputs that 809 built and 815 also reads, and is worked after 817 and never beside 815; 817 is the append-path defect that the cross-vendor reading commissioned by 816 turned up — the activity journal can hand out a fenced sequence twice and weld two records into one line — so it edits the very journal core 809 built and 815 reads, and is worked strictly after 816 and never beside 815; 816 is the review debt that 809's landing left behind — a HIGH point ticked without a recorded verdict on its merged, mixed-authorship range — so it reads the very branch 809 produced and stands in front of the release because the criticality gate refuses every turn end until it is settled; 815 is the second finding of 21.08.2026's cross-vendor review of 809 — the standstill classifier's quadratic read path and the journal's missing retention — so it edits the very core 809 builds and is worked strictly after it, never beside it; 814 is the drained finding of 21.08.2026 — the boundary's dictated handover card claims nothing is running while it has just reported transferable delegated work — so it edits the boundary card composer that 790 and 800 also own and is not worked beside them; 811, 812 and 813 are the three proven levers cut out of 809’s merged spec — the handover that travels only by scheduler tick, the writer that vetoes by merely existing, and the CI wait that dies with its turn — so they edit the launcher start decision, the ownership fence and the CI-guard core that 809 only measures, and they are worked strictly after 809 and in that order; 810 is the unverified half of 809's four-eyes sweep — sixteen candidate defects in the CI guard and the launcher that the merge deliberately scoped out, so it edits the same two cores 809 rewrites and is worked strictly after it; 809 is the user's ranking decision of 21.08.2026 and stands at the very front of the queue — the batch's own standstill has never been measured, so it reads the gaps on main and classifies them before it removes any of them; it edits the launcher's start decision that 795 also touches and the wait path 744 and 752 measure, so it is worked before them and never beside them, and the stage-2c request of the folded point 808 is carried by 813; 807 is the branch nobody points at — pushed work for an open point that the brief and the commissioning step never mention, so it edits the point-brief and commission-guard paths this bundle already owns and is worked with 708, which builds the commissioning command it belongs in; 806 sharpens the repair-loop reading point 772 just landed — it edits the same detector and is worked after it, never beside it; 805 is the same push gate refusing for a second, unrelated reason — the integrity teardown reading a delegated author's own branch commit as repository damage — so it edits the gate path 803 owns and the fixture/integrity path 801 owns, and is worked with 803 rather than beside it; 804 is the gap the review of 784 left open — a merge loses its attribution as soon as its parent leaves the analysed set, and the `--since` narrowing the refusal advises is what causes it — so it edits the same review-planner core 783 and 784 own and is not worked beside them; 803 is the push gate running the full unit layer while a delegated author holds the same machine — it edits the git-hook path 801 also owns and it is measured against the delegated-authoring lanes 753 and 780 touch, so it is not worked beside those; 802 is the stale user-confirmation wording point 794's review found one file over — it edits the same doc-budget module 762 owns, so it is worked with 762 or after it, never beside it; 801 is the unit suite writing into the live repository — it moved main, set core.bare and left fixture branches behind, so it edits the test fixtures and the git-hook path this bundle already owns and is worked before anything that pushes; 798 is the same class as 796 one step earlier — a report that turns a finished delivery into a non-delivery, so the two are read together and it is not worked beside 796; 797 is the other half of 785 — the header figure is never refreshed during a turn, so it shares that path and is read beside it; 795 and 796 are the two findings of the 20.08.2026 night — the launcher that read a lock file as proof of life and started a second writer on main, and the authoring run that reported nothing authored over 118 finished lines — 795 edits the launcher and the ownership fence this bundle already holds, and 796 the authoring path 753 and 780 also touch, so neither is worked beside those; 738–748, 750–752 and 756–759 are the context-ceiling programme of 19./20.08.2026 — they share the fence, the watermark core and the boundary, so they are not worked in parallel; 757 cut the per-turn start floor the others measure against and 761 owes its measured proof, and 758 holds the fence in observation until it has; 762 is what 761's ceiling confirmation turned up and edits the same doc-budget module, so it does not run beside 761; 763 executes the three document cuts the user released that 757 could not take with it; 764 is the same cut's method failing — a rule deleted as guard-covered that no guard covered — and 765 the carrier defect that hid the ruling behind it, so both are read beside 763; 753 is the authoring-routing half of the same session economics; 771–775 are the findings of the 20.08.2026 morning — the claim guard narrowed until it caught nothing, an owner that repaired its own mechanism instead of handing over, a test red in every worktree, the status path that refuses the owner its own reading, and the owed answer to the commit-time registration review — and they name this bundle in their own text; 780 is the same morning's lane defect, found while commissioning 775 — the commission ledger cannot be written from an isolation worktree, which is where every delegated author works — so it is read beside 775 and touches the authoring path 753 also edits; 781 is the duplicated main-checkout helper that 773's fix must choose between, so it is settled before or with 773; 790 and 791 are the two findings of the 20.08.2026 evening — the dictated handover card naming a session id that died with a `/clear`, and the closing waiver that has to be typed thirteen times and once has to sound like a run — both edit the boundary and closing paths this bundle already holds, so they are read beside 787; 789 is the ranking rule for a point the MACHINE files — it extends the append gate of 590 and reads the release anchor the board queue owns, so it is not worked beside 787, which edits the same board path; 785 is the truth of the chat header's context reading, which shares the header path 764 enforces and is therefore read beside it; 787 is the contradiction between the boundary's dictated handover card and the board's publish gate, found at 782's own boundary — it edits the boundary and board paths 772 and 434 also touch, so it is not worked beside them; 786 is the same session's measurement defect one level up — a tick that hides whether a point was delivered or folded — and it reads the archive that 783 and 784 plan reviews over, so it is not worked beside them; 784 is the blockage 783 measured but deliberately did not widen itself to remove — it edits the same review-planner files, so it is not worked beside 783 and it is ranked early because every landing adds another trailer-less merge to the plan it refuses; 646 is the doctor-side twin of 629 — the same deletion reached by a different route; 649 holds the holes the fifth review found in 629's own lock protocol; 553 is 373's measured successor lever; 556 is the lease half of the same singleton family; 881 is the context fence which, armed, would refuse the commands that end a session — the landing, the finding carrier, the claim withdrawal and the preflight the boundary prescribes — and which on a machine with no incident series refuses every start at three per cent; it edits the fence core and its guard, which live with this bundle, and it must be settled before its arming point lands rather than before the queue; 879 is the write fence walked through behind `--help` — the landing chain classifies as read-only while it merges, ticks and pushes, and eight ref-moving git subcommands are invisible to it, so a session with no batch lock can move refs/heads/main; it edits the command classifier that nothing else in the bundle touches and stands BEFORE the release, which is cut on main; 878 is the CI guard that throws away a decided red verdict when persisting it hits a lock timeout, and whose suite reads its own source instead of running it; 882 is the document budget that a deleted or mis-keyed file switches off silently; 886 is the preflight case that fails on the first run after every push, because the CI probe inside it burns a network timeout longer than the case allows — it edits the preflight test and the CI-probe bound that 878 also reads, so it is not worked beside 878, and it stands behind the release because the machine filed it; 901 is the superseded CI run reported as a failure — a run the workflow's own cancel-in-progress killed because a newer push reached the same ref is classified `failed`, becomes a red verdict, and hands the successor session a repair order over a green tree, measured 25.08.2026 on the start of the session that landed 892, whose named "failure" was superseded by a commit whose own run concluded success; it edits the CI status guard core and the autostart handoff that 851 and 878 also read, so it is not worked beside them, and it stands behind the release because the machine filed it) — the rest landed 30.07.2026 (329, 396, 399, 409, 426, 427, 429, 431, 433, 458) and 08.08.2026 (373). 906 is the delegated author's interim push that cannot recover a branch the runner rewrote itself, leaving the remote blind for the rest of the run while its report names an undefined range and calls already-transferred work local-only; it edits the authoring wrapper's push path, which no other open point owns, so it is worked whenever the bundle has room, and it stands behind the release because it loses nothing. 907 is the second mouth of the gap 879 closed: a call that hands its text to an interpreter — through a here-document or a pipe — still passes the write fence as a read, so an unlocked session can move refs/heads/main by that route; it edits the same command classifier 879 did and nothing else in the bundle touches, so it is worked whenever the bundle has room, and it stands BEFORE the release for the reason 879 did, because the release is cut on main. 908 is the parallel-session detector counting the owner’s OWN declared authoring child, so every turn of every delegated build is halted and charged a doctor gate that answers “consistent” by construction; it edits the detector core and the in-flight declaration that 515, 823, 841, 849 and 853 also read, so it is not worked beside them — and 849 in particular fixes the neighbouring class in the same reading, so 908 follows it. It stands behind the release because the machine filed it and it loses no work. 909 is the tests' type-check that has been red on main for a week while every signal said green, because no path that the batch actually walks runs it — CI does not, and neither does the landing gate; it edits the verify runner's tier map and the CI workflow — the tier map 1026 also rewrites, so the two are never worked beside each other and 909 goes first, because 1026 narrows a map that must first be reached at all — so it is otherwise worked whenever the bundle has room, and it stands behind the release because the three errors it hides are in a test file and in test-only import declarations, not in the shipped game. 910 is `dashboard-guard` resolving the board path against whatever repo root it happens to run from, so a session standing in a worktree — the NORMAL case of our branch procedure — is refused with a repair that provably cannot satisfy it; it edits the dashboard guard that 434 and 787 also touch, so it is not worked beside them, and it stands behind the release because it costs turns rather than work. 911 is the escape drill's launcher shape being a comment rather than a coupling, so a launcher that moves its worker back to `pipe` leaves the drill measuring the obsolete shape and passing; it edits `scripts/detached-escape-drill.mjs` and the launcher spawn path that 834 landed and nothing else in the bundle owns, so it is worked whenever the bundle has room, and it stands behind the release because the risk it removes is drift rather than a present defect. 912 is the takeover drill that leaves its daemon and its sandbox behind whenever the drill process itself dies — the one case a drill about surviving process death cannot exclude — while `batch-doctor` reports `strayProcesses=0` over the live leftover; it edits `scripts/batch-daemon-drill.mjs` and the doctor’s stray-process detector, and 911 owns the neighbouring drill file, so it is not worked beside 911, and it stands behind the release because it leaks processes rather than work. 918 is the pair of gaps the 25.08.2026 cross-vendor audit found in the durable lane's activation seam — the interlock verifies the drain mode as a bare string without evidence, and the daemon has no atomic close-admission operation for a boundary drain to use; it edits `scripts/durable-lane-flag-core.mjs` and the daemon control plane that 891 and 894 landed and that 911-913 also touch, so it is not worked beside them, and it stands directly before 676 because 676's drain enforcement builds on the seam it adds. 921 is the era rule that condemns its own history: the well-formedness check for a review row demands an identity object a later rule introduced, so rows the recorder itself wrote before it are read as hand-written, and the four-eyes gate answers UNREVIEWABLE for every commit that depends on them — measured 25.08.2026 at the point-918 boundary, where it blocked the turn and named a repair that exists as no command. It edits `scripts/mechanism-review-core.mjs`, the ledger core that 783, 784, 804, 856 and 870 also own, so it is not worked beside them, and it stands at the FRONT of the queue because it halts every turn that ends on main. 922 is the same class one guard over, and 910 is its sibling: the Stop hooks are registered with relative commands, so a session standing in a worktree runs THAT checkout's guard, where the untracked baseline does not exist — the four-eyes gate then reports a missing baseline and prescribes a merge that is already done. It edits `scripts/mechanism-review-guard.mjs` and the state resolution 910 changes in `dashboard-guard`, so the two are worked together rather than beside each other, and it stands directly behind 910 for that reason. 924 is the fast gate calling the unit suite red where the same command in the same tree runs it green, blocking a push on a runner that never finished rather than on a test that failed — it edits `scripts/pre-push-gate.mjs` and its core, which no other open point owns, so it is worked whenever the bundle has room, and it stands behind the release because it costs turns rather than work. 925 is what point 676 left unmeasured: no production caller journals the dispatcher's underutilization reason or any metric, eleven of the twelve failure drills assert decision functions on hand-built inputs instead of the real path, and the measured trial against a recorded baseline day is still owed. It edits `scripts/batch-dispatch.mjs`, the daemon's metric command, `scripts/batch-daemon-failure-drills.mjs` and `scripts/batch-board-core.mjs` — the durable-lane plane 676, 834 and 889-895 built — so it is not worked beside any of them, and it stands behind the release because the lane it unlocks stays switched off until it lands. 929 is the board-core hardening the eighth cross-vendor round on 713 measured but did not cause — one work-order snapshot per publish, a readable failure for an unreadable work order, an all-or-nothing focus transition, point numbers read from card heads only, structural damage refusing instead of answering "nothing there", and the honest zero state recognised by its own text; it edits `scripts/board-core.mjs`, `scripts/board-publish.mjs` and `scripts/focus.mjs`, the very files point 713's branch changes, so it is worked strictly AFTER that landing and never beside it. Plus 939 (939 is the dev-server transient the verify lane records as an ordinary console red — Vite's optimizer re-bundled mid-run and the 504 it produced blocks the gate exactly as a product defect would, although the house rule says an environment transient fails soft; it warms the server before the suite and teaches the console capture the difference, touching the recorder's capture path that 460 also reaches, so it is not worked beside it), 960, 961 (960 is the owner lookup that no filed finding gets: with 314 open points, three of four observations carried on 27.08.2026 already had owners — 937, 906 and 729 — and nothing between `finding.mjs --record` and the append asks who owns this, so a duplicate point is the normal case with a delay; it edits the finding CLI and `tasks-source.mjs` that no other open point of this bundle touches, so it may run beside any of them. 961 is the `commit-msg` hook checking two properties of a message and not its shape, so a body written without git's blank separator lands as a 400-character subject and every `--oneline` reading of the project renders one commit as a paragraph — measured on two commits of 27.08.2026 that stay as they are, because rewriting published main history costs more than the defect; it edits the versioned hook and the commit-scope guard beside it, which no other open point owns), 972 (972 is the review harness cutting a file from the material without declaring it, so the reviewer charges the commit with its absence — the measurement point 971 named as the condition that promotes its harness lead to a finding, taken 27.08.2026 on the same commit; it edits `scripts/review-sol.mjs`'s plan and manifest, which 933 also touches and 870, 783, 784, 804 and 856 reason about, so it is worked with 933 and never beside them), 974 (974 is the criticality gate reporting a point's debt as files the point never touched — measured 27.08.2026 against point 966, whose branch touched neither named file — and the set grows with every commit, so the gate blocks every turn end and cannot converge; it edits `scripts/criticality-review-guard.mjs`, which no other open point of this bundle owns, so it may run beside any of them, and it stands early because it blocks the turn end itself), 975 (975 is the four-eyes debt no vendor may clear — the fix for an Anthropic-authored contribution was authored by Sol, so the gate demands a Sol verdict on a state the harness refuses to let Sol read, measured 27.08.2026; it edits `scripts/mechanism-review-guard.mjs` and the record path that 870, 856, 783, 784 and 804 also reason about, so it is worked with 870 rather than beside it, and it stands early because it blocks the turn end itself), 981 (981 is the fifteen unfixed findings of the 28.08.2026 cross-vendor rule-corpus review, recorded as section H of docs/rule-corpus-audit.md — three of them confirmed against the files, including two dead routing sentences the code already overrides; it edits the memory corpus and that audit document, which no other open point of this bundle touches, so it may run beside any of them), plus 990, 991, 992, 993 (the four defects the eleventh and twelfth cross-vendor rounds over point 734 found outside what that point promises: 990 lets a ledger entry declare the ROOT its downstream sentence depends on, 991 gives a run record an identity of its own instead of its content hash, 992 scopes the enrichment console charge to the feature level its own evidence names, and 993 records a crash the process survived — today such a run stores neither its reds nor its crash flag and is counted as a backend's picture proof. All four edit `scripts/render-verify-core.mjs`, `-charges.mjs`, `-recorder.mjs` or `-guard.mjs`, the files point 734's branch rewrites and 460 and 514 also reach, so none of them is worked before 734 lands, nor beside 460 or another of the four; 993 stands first of them because it is the one path on which the render gate accepts a crash as a green), 994 (994 is the writer veto that outlives both its clock and its process — the launcher learns of an expiry only at its next 900-second tick, and never asks whether the writer still runs; it edits the launcher start decision that 809 and 811 also own, so it is worked after them and never beside them), 995 (995 is the last width left in the charge ledger after point 734 scoped every entry to its lane: three entries excuse a named frame or check while their evidence covers one way it reds, and only a NEW recording can close that — it edits `scripts/render-verify-charges.mjs`, which 990 also reasons about, so it is worked after it and never beside it), 996 (996 is the malformed byte sequence that renames a recorded red while the run still reads complete — the exact half is marked and this one needs a decoder reading that can tell a substitution from a printed character; it edits the tap in `scripts/render-verify-recorder.mjs` that 993 also changes, so it is worked after it and never beside it), 997 (997 is the measurement the context order rests on and does not have — frequency counts for 901 and 705, a balanced comparison base, and the classification the fence's own observations still owe; it reads the measurement tooling 764 and 785 own rather than the points it would reorder, so it may run beside any of them, and it moves in the queue only when the user rules on that order), 998 (998 is the mechanism gate billing a merged contribution PER COMMIT, so a 59-commit branch owes some 295 review runs for files its own planner reviews once at the end state; it edits the clearing rule in the mechanism-review guard and core that 856, 870 and 983 also reach, so it is not worked beside them, and it stands before the release because it blocks the turn end of a landed point), 1000 (1000 is the pair of waved-red recorder cases that construct a console identity the recorder cannot mint and name a production pair their own comments retract; it edits the render-verify recorder's test file that 996 also rewrites, so it is worked after 996 and never beside it, and it stands behind the release because it is a truthfulness defect in the suite, not in the mechanism), 1003 (1003 is the bundle membership reader itself: it takes point numbers out of free prose and four cross-vendor rounds refused every date filter written for it, because the points cell now carries paragraphs and a regular expression cannot tell a reference from a measurement. It edits `scripts/bundle-first-core.mjs`, which no other open point of this bundle touches, so it may run beside any of them, and it stands behind the release because the guard errs toward allow), 1004 (1004 is the residue of point 1003's nine review rounds — the marker reader's Markdown contexts, the silent choice between the two home authorities and the last fail-open shape; it edits the very reader and test 1003 rewrites, so it is worked AFTER 1003 has landed and never beside it) Plus 1005 (1005 is the board having no card for bookkeeping that closes no point: while the idleness card stands, board-first-guard refuses every state-changing call and offers only remedies that need a point number; it edits the board-first core and the board's card composer, which no other open point of this bundle touches today), 1006 (1006 is the mechanism ledger losing an older cross-vendor read when a later same-vendor clearance covers the same files — measured 29.08.2026, where the gate reported a missing independent review that stood in the ledger; it edits the ledger's file-level resolution that 916, 983 and 988 also reason about, so it is worked after them and never beside them), and 1007 (1007 is the measured trial split out of 925 on 29.08.2026 — the campaign rather than the apparatus: a recorded baseline day and a trial interval of real durable-lane operation, whose verdict decides the switch. It runs the machinery 925 builds and edits no code of its own, so it waits for 925 and runs beside nothing), and 1008 (1008 is the review router and the review ledger each forbidding what the other requires, so two passes of a sixteen-pass plan can be cleared by nobody — measured 29.08.2026 on the 686+687 landing range, where the router named Opus 5 and the ledger refused it as the tip commit’s own vendor; it edits the review planner and the ledger’s eligibility rule that 783, 784, 804, 856, 870 and 1006 also own, so it is worked after them and never beside them, and it stands early because it blocks the four-eyes record of a landing that is otherwise ready), and 1009 (1009 is the benchmark run borrowing the world — seed, day, travel zoom and speed, the SSAO switch and `Math.random` itself — and returning none of it, measured 29.08.2026 and already red on `main` with no point and no charge owning it; it edits `scripts/verify/benchmark.mjs`, which no other open point of this bundle touches, so it may run beside any of them), and 1010 (1010 is the Ctrl-label fusion check naming a TICKED point, so its red has no owner and no charge can be written for it; it edits `scripts/verify/polish.mjs` and the charge table, which 1009 also touches, so it is worked after 1009 and never beside it), and 1011 (1011 is the WebGPU compatibility lane's async-pipeline and command-buffer console errors, which two runs of 29.08.2026 emitted and which no charge names — a charge was written for them and WITHDRAWN on 30.08.2026, because no matcher narrow enough to exclude an unrelated failure on the same lane could be written from the record; it edits `scripts/render-verify-charges.mjs` and its core test, which 990, 995, 1009 and 1010 also reach, and it waits for 990 in particular, which owns letting a charge name the root its downstream sentence depends on), and 1012 (1012 is the benchmark demanding GPU timestamps from an adapter without the `timestamp-query` feature, a check that cannot pass on the lane it runs on; it edits `scripts/verify/benchmark.mjs`, the same file 1009 rewrites, so it is worked after 1009 and never beside it), and 1014 (1014 is the Anthropic share of the batch having no lever left except ending the session — `prefer-sol` already routes everything routable, and what remains is what §6 never routes away plus the serving model itself; it depends on the durable authoring lane, so it waits for 834 and runs beside nothing)., and 1015 (1015 is the half of the red-charge table that still names a check by its prefix and excuses whatever else fails under that name — fifteen entries the cross-vendor review of 30.08.2026 named while reading the whole end state rather than the diff; it edits `scripts/render-verify-charges.mjs` and its core test, which 990, 995, 1009, 1010 and 1011 also reach, so it is worked after them and never beside them), and #1021 (1021 is the stale chat timestamp that still costs the user a second message — nine `timestamp-guard` blocks across two sessions on the morning of 31.08.2026, each one delivering the answer he had already read and then a short acknowledgement about the stamp, which is the half of point 403's fix that did not hold; it edits the chat header's injection and the guard around it that 764 and 785 also reach, so it is worked with them and never beside them, and it stands behind the release because a stamp re-measured immediately before the closing reply avoids it every turn), #1029 (1029 is the bundle table's own exclusions written on one side only — measured 31.08.2026 over nine pairs, including two written during the repair of a tenth: the dispatcher reads ONE entry, so a point whose counterpart stays silent is commissioned straight into the collision the split exists to prevent. It edits this table and the guard that reads it, which no other open point of this bundle writes, so it may run beside any of them, and it stands before the release because every parallel commissioning takes this path), #1032 (1032 is the turn-end chain refusing the same finding forever: a red Stop guard restarts the turn instead of ending it, nothing counts consecutive refusals of the SAME finding, and each one spends a user-visible answer — measured 31.08.2026 as ten near-identical replies before the user asked what was happening. It edits the turn-end guard chain that 719 reasons about and 958 measures from the emergency side, so it is worked with those and never beside them, and it stands before the release because it is a standstill hidden behind activity), #1033 (1033 is the freed lock landing in the window a person is talking to: the turn-end guard acquires for every live session, so a dead owner hands the batch to the user's interactive window, which then reads its own stand-down notice as still true — measured 31.08.2026 at 23:03. It edits the singleton acquire that 660, 964 and 1025 also own, so it is worked after them and never beside them, and it stands before the release because it decides who the unattended batch belongs to), #1034 (1034 is the four-eyes gate printing a repair command its own planner refuses — measured 01.09.2026 over three of the thirteen blocked contributions, two of which fit in ONE round where the gate demands the fifth of thirteen; it edits the mechanism-review guard and the review planner that 783, 784, 804, 856, 870, 1006 and 1008 also own, so it is worked after them and never beside them, and it stands behind the release because the machine filed it), #1036 (1036 is the four-eyes ledger debt the merge-authorship repair left standing on `main` — two stale pass splits with no runnable command, two pre-repair rows for the landing merge that still outrank the attributed ones, and the repair's own Sol passes. It edits the review ledger and planner that 783, 784, 804, 856 and 870 also own, so it is not worked beside them. It also reaches `scripts/verify/baseline-classify.test.mjs`, which 1035 changed, so it does not run beside 1035 either — an exclusion that crosses into Testinfrastruktur and is therefore written into 1035's entry as well, because the dispatcher reads only the entry of the point it is commissioning (point 1029) and a one-sided exclusion schedules the collision from the other side. It also reaches `scripts/mechanism-review-guard.mjs`, which point 1034 owned; 1034 is CLOSED, so that one needs no reciprocal line and is recorded here only so the next point to touch that file can see who was there. It stands BEFORE the release because the gate refuses every merge while it holds), #1037 (1037 is the daemon drill that signals its teardown without ever confirming it — it requests a shutdown, sleeps a fixed 300 ms, SIGKILLs whatever is left and returns, so the pid it discloses as torn down may still be dying when the caller reads it; that reddened CI on 01.09.2026 and would hide a real leak on a fast machine. It edits `scripts/batch-daemon-drill.mjs` and its test, the same files 913 and the 834/889-895 seams reach, so it is not worked beside any of them, and it stands directly behind 1036 because CI on `main` is red until it is fixed), #1038 (1038 is the launcher's writer probe that can never call a feature writer dead — `registeredFeatureWriters` passes no process identity into `checkAgentOutput`, so the one branch of `agentOutputVerdict` that returns `dead` is unreachable by construction and a writer that died with its session keeps vetoing the launcher for the full 30-minute grace while the declaration probe already refutes it; measured 01.09.2026 as 24 minutes of standstill. It is the half of point 874 that 874 never reached: 874 taught the DECLARATION path to probe its recorded pid, and this is the same defect in the launcher's REGISTER path. It edits `scripts/batch-in-flight.mjs` and reads `scripts/batch-autostart-core.mjs`'s `launcherStartDecision`, so it is not worked beside 552 or 625, which reach the in-flight probe, nor beside 658, 678, 760, 799, 810, 818 or 901, which reach the launcher core; it stands with 1037 at the front because a wedged launcher stops the batch outright), #1050 (1050 is the pair of ownerless polish reds of 03.09.2026 — a framing failure ("crop off-frame") and a duration ("3683 ms"), charged here so the picture gate can be read at all, and neither yet separated from the host it was measured on; the point owes the throttle probe that says load or defect for each section. It edits `scripts/render-verify-charges.mjs`, the same table 1015, 1017, 1031 and 1035 reason about, so it is worked with them and never beside them, and it stands behind the release because the machine filed it) |
| **Modell & Wächter** | J | Model and guard chain | #1019 (1019 is the do-not-merge verdict booked against the FILE rather than the finding, so it blocks every contribution that touched that file — 25 of 30 measured on 30.08.2026 had never been read by it — while answering a finding on a hot file owes a counter-read of its own, so the debt rose from 4 to 30 to 40 across eight commits that each closed a real defect; it edits the four-eyes gate and its ledger reader that 887, 888, 977, 916 and 915 also own, so it is worked after them and never beside them, and it stands before the release because it shuts every turn end, the closing run included), #919, #916, #915, #917, #914, #903, #905, #896, #900, #309, #355, #397, #425, #437, #438, #457, #468, #534, #535, #536, #537, #538, #540, #560, #561, #613, #630, #631, #632, #634, #638, #639, #640, #726, #862, #863, #495, #497, #542, #559, #596, #598, #599, #609, #650, #652, #669, #677, #678, #693, #715, #718, #722, #729, #735, #736, #737, #778, #788, #792, #872, #873, #875, #876, #877, #880, #883, #979, #999, #942, #962, #970, #984, #986, #987, #1001 (979 is the gate holding a refusal no command can answer — the printed plan records at the refusal's own sha, which by the gate's own rule clears nothing, and every plan containing the refused commit assigns its files a reviewer from the vendor that may not review them; it edits the review core and its pass planner that 880, 916 and 957 also own, so it is worked before them and FIRST in the order, because until it is fixed every turn end meets a refusal it can only defer; 916 is the four-eyes tor blocking every landing session for reasons outside its reach — hand-written ledger rows whose timestamps cannot be ordered, plus a split review recorded only half; it edits the review ledger's reader that 915 and 900 also own, so it is worked before them and FIRST in the work order, because until it is fixed every landing meets a refusal it can only waive; 873 is the pre-action oracle contradicting the gate it predicts — guard-preflight reports the mechanism four-eyes gate as blocking where the guard itself measures a review gap and lets the turn end; it edits guard-preflight beside the same gate 872 touches, so the two are worked together; 872 is the merge resolution credited to the side that was merged in rather than to the model that resolved it — found in the cross-vendor read of 870, together with the measurement that ten of the last twelve merges on main carry no author trailer at all; it edits the review planner and the §6 trailer rule that 856 and 854 also own, so it is not worked beside them, and it stands behind the release because the machine filed it; 792 is the switch of 788 judging the past — it edits the same guard core, so it is not worked beside anything else that touches it; 788 is the switch the user ordered on 20.08.2026 — one place decides whether Fable is used at all, and it edits the author routing, the model guard and the merge recorder that 778 answers the review of, so the two are not worked beside each other; 534–538 are the audit findings of point 297 — worked BEFORE the rest of the bundle, because they decide what the four-eyes gate and the review schedule see at all; 880 is the four-eyes gate itself — five measured routes clear a review duty without an independent reader, plus a gitignored baseline that grandfathers the whole debt on a fresh clone; it edits the review core and its guard that 872 and 873 also own, so it is not worked beside them, and it stands BEFORE the release because every clearance recorded until it is fixed may be a self-review; 875 is the commit gate that refuses every commit when the Fable switch file cannot be read, so it edits the switch path 788 and 792 own and is not worked beside them; 876 is the guard-health check that clears a dead enforcer on a bare substring and raises findings from settings it never parsed; 877 and 883 are the two halves of the urgency parser — negation read by proximity, a question read as a claim, and a baseline that proves membership instead of the invariant — so they are worked together and behind the release) 900 is the review recorder that answers a failed identity check with the advice to pass the two flags that were already passed, discarding the measured reason it holds one layer down; it edits the mechanism-review recorder and the authorship check this bundle owns, so it is not worked beside anything else on that path, and it stands behind the release because it costs minutes, never correctness. 905 is the cost of the fallback 903 introduced — the Git-only file-set measurement re-lists every merge on main once per review row, measured 0.22 s to 0.65 s on the Stop hook and 16.2 s over the whole ledger; it edits the very criticality guard 903 wrote it into, so it is worked after 903 and never beside it, and it stands behind the release because it loses nothing. 917 is the branch hygiene the guard stopped measuring — a closed point's worktree and branch outlive the merge CLAUDE.md §6 says ends them, and `branch-hygiene-guard --status` answers a foreign lock by reporting no state at all instead of reporting it without acting; it edits that guard alone, so it is worked beside nothing else on that path, and it stands behind the release because it costs no correctness. Plus 942 (942 is the criticality gate's second way out with no command behind it: the guard reads a findings-filed receipt and its refusal names it, but nothing in scripts/ writes one, so an honestly filed refusal is answerable only by hand-appending JSON to a tracked ledger — done once on 26.08.2026 as evidence, not as a precedent; it edits the criticality guard and the review CLI beside it, which no other open point of this bundle touches today) and 943 (943 is the pulled-forward work-off of the unreviewed four-eyes backlog — decided 26.08.2026 under the user's delegation; it performs the owed reviews and fixes through the recording commands and edits no mechanism, so it is worked BEFORE 916 changes what the ledger reader accepts, never beside it, and stands at the front of the queue) and 957 (957 is the four-eyes gate demanding a range nobody can read: main's baseline sits 226 mechanism commits back, that range plans 115 passes over 15.1 million characters, and the gate's own ledger alone needs 489311 characters of diff that fit no round, so the gate lives suspended while the debt compounds. It rescopes what the gate demands - the contribution rather than an unbounded range - and so edits the review planner and its guard that 916, 872 and 880 also own, which is why it is worked before 916 changes what the ledger reader accepts and never beside them) and 962 (962 is the delegated lane's readiness report charging the lane with the main commits its own brief told it to merge in: the check walks base..HEAD, so the mandated `git merge main` puts the supervising session's own correctly trailered commits into the lane's range and each is flagged twice — measured 27.08.2026 as eight false entries of a nine-entry PROBLEMS block whose ninth was 729's skipped-cases misreading, leaving the block entirely noise. It edits `scripts/author-sol-core.mjs`, the same readiness reader 729 corrects, so the two are worked together and never beside each other) and 970 (970 is the canonical-trailer check the 27.08.2026 sweep demands — two commits name Opus 5 without the Claude prefix and two carry Sol under the anthropic domain, all allowed models in drifted spellings that textual trailer matching silently drops from counts and reviews; it edits the versioned commit hook's trailer check beside the commit-msg shape defect 961 owns in its own bundle, touches no open point of this bundle, so it may run beside any of them, and it stands behind the release because the machine filed it), 982 (982 is the reviewer trailer §6 now allows and no reader can tell from an author trailer: a commit naming its cross-vendor reviewer reads as authored by both vendors, so no reviewer is independent and the contribution cannot be reviewed at all. It edits the trailer readers in model-guard-core and review-sol-core that 977 also reasons about, so it is worked before it and never beside it, and it stands before the release because it holds an open do-not-merge on main), 983 (983 is the range-global split witness measured on 28.08.2026: one file-scoped pass row at a descendant sha deletes every pass-less cross-vendor review in its range, so reviewed work is reported as self-reviewed and the printed remedy asks a vendor to review its own commits. It edits the same evaluation core as 979, so it is never worked beside another point touching that file, and it stands before the release because it blocks every turn end), 984 (984 is the landing that writes its author model at the wrong vendor address — every tick commit records GPT-5.6 Sol at an Anthropic no-reply address, which the review recorder refuses outright; it edits the trailer derivation in model-guard-core that 982 just settled, so it is worked after it and never beside it), 986 (986 is the criticality gate charging a point for every unrelated main commit that landed between its lane base and the reviewed sha — measured as two review rounds spent re-reading another point’s finished work; it edits the file-set measurement in the criticality guard that 905 and 942 also own, so it is worked after them and never beside them, and it stands behind the release because it errs towards more review, never less), 987 (987 is the receipt check that blames the wrong model when a routed reviewer answers but its receipt carries no text result: the name matched, yet the check reports "a different model answered" instead of "this receipt cannot be verified", and reverifyReviewerAgreement carries that accusation onto a stored record. It edits the mechanism-review recorder and its authorship check that 900 also owns, so it is worked after 900 and never beside it, and it stands behind the release because it is fail-closed and misleads only the diagnosis), 988 (988 is the append-only review ledger that collides with itself on every second landing — nine of the last thirty merges on main stopped there, and the standing repair is a hand edit of machine-written evidence whose most obvious form deletes recorded reviews. It changes how Git merges that file rather than how the ledger is read, so it touches no reader 916, 915, 900 or 956 own and may run beside any of them, and it stands before the release because every landing pays for it), 1001 (1001 is the delegated lane judging commits it only MERGED IN as its own lane's work — measured 28.08.2026 on the point-993 and point-925 runs, where the whole PROBLEMS block was false positives about correctly trailered `main` commits; it edits the closing report in `scripts/author-sol-core.mjs` that 962 and 729 also own, so it is worked with them and never beside them), #1028 (1028 is the raised escalation threshold that never reaches the points already in flight: the examination record in `scripts/author-routing-core.mjs` carries no round and `nextAuthoringStep` gates on the bare `!history.examination`, so a point examined at the old round-4 boundary walks unexamined into the round-9 escalation — the very case the raise was built for. It edits `scripts/author-routing-core.mjs`, which no other open point of this bundle writes — 962 and 1001 edit `scripts/author-sol-core.mjs` and 736 re-cut its own text rather than this core — so it may run beside any of them, and it stands before the release because it decides how the scarcest model lane is spent), #1030 (1030 is the four-eyes gate reporting the opposite of what it measured: a recorded do-not-merge whose relation to a file is co-touching is dropped as a refusal but still counted as a reading of that file, so the refusal clears the very artefact it refused — reachable through end-state artefacts and through every carried row. It edits `scripts/mechanism-review-core.mjs`, the ledger reader 956, 887 and 888 also reason about, so it is worked with them and never beside them, and it stands before the release because every landing passes through this gate) |
| **Testinfrastruktur** | K | Test and verification infrastructure | #1044 (1044 is the crashed suite whose stack never reaches a log — run-all captures a suite's output and echoes its tail only when there was no FAIL line, so a suite that reds and THEN dies loses the death; it has blocked two landings. It edits `scripts/verify/run-all.mjs` and `run-logged.mjs`, which no other open point of this bundle writes — 1043 edits a polish section, 1026 the tier map — so it may run beside any of them, but NOT beside 948, which edits the tier's own reporting through the same runner), #1043 (1043 is the speech-label check that rests on ONE figure — five of the ten candidates it offers are deterministically unhittable, so a single flip decides the run and it has already cost a foreign candidate two SUSPECT records. It edits the `speech-hypothesis` section of `scripts/verify/polish.mjs`, which no other open point of this bundle writes — 1015, 1017, 1031 and 1035 all reason about the CHARGE table instead — so it may run beside any of them), #1040 (1040 is the CI red on the 1036 lane — `mechanism-review.mjs --list` dies on a signal; it will edit the mechanism-review tooling other points of this bundle also reach, so it is not worked beside them), #295, #330, #387, #418, #455, #456, #460, #464, #466, #532, #549, #557, #563, #564, #566, #567, #568, #569, #570, #571, #572, #573, #574, #641, #642, #647, #498, #506, #507, #510, #514, #518, #521, #552, #558, #595, #597, #615, #620, #622, #626, #627, #643, #668, #670, #695, #776, #829, #857, #926, #927, #948, #949, #951, #968 (926 and 927 are the two pre-existing reds the point-595 ladder proof read out loud on 26.08.2026 and baseline-classified against main: the world suite’s Victoria Falls frame whose subject sits off the left and bottom edge, and the F6 report archive that carries no PNG. Both edit `scripts/verify/*.mjs` together with 595 and 566, so neither is worked beside them; 927 also reaches the F6 assembly in `src/`, which no other open point of this bundle touches, so it may run beside 926. 857 is the acceptance-pointer checks that match nothing and fail vacuously — the parser still reads the pointers out of CLAUDE.md §7.1 after that section moved its conditions and evidence into the two detail documents, so six checks judge zero pointers and redden the docs suite on every tree; it edits the very docs suite 829 and 730 own, so it is worked with them rather than beside them, and it stands behind the release because the machine filed it; 829 is the calibration suite that measures the repository it runs in — it reddened main on 22.08.2026 in a two-commit CI checkout that has no merge parents, and it edits the very suite 730 built, so it is worked after that and never beside it; 564/566/567 all came out of the point-342 verification: what a red is believed to mean, what a repair costs, and what a killed session leaves running), 948, 949, 951 (948 is the red vitest type-check that reached `main` and stopped the verify tier at `test-types`, so no point could reach its suites — it edits the tier's own reporting, which 595 and 926/927 also read, so it is worked with them rather than beside them; 949 is the class behind the fixture that read the LIVE checkout because its module resolved repository paths from `import.meta.url`, and it sweeps `scripts/` for the same shape, so nothing else that moves a path resolution runs beside it; 951 is the pair left over from the 90 MB model download — the voice cache that reports zero hits against a COMPLETE cache, and the rule that a suite reporting zero checks must be a LOUDER verdict than one failing a check; it edits the verdict layer 595 owns, so it is worked after it), 968 (968 is the picture gate reopened by a suite that draws no game view — that suite cannot record a covering run by construction; it edits the render-path classification of the picture gate, which no other open point of this bundle touches, so it may run beside any of them), #1016 (1016 is the run that REPORTED its reds and was recorded as a crash — four such records of 30.08.2026 block the picture gate in the one way no charge and no owner can lift, because a crash is defined to carry no red anybody can own. It edits `scripts/render-verify-recorder.mjs`, the classification no other open point of this bundle touches — 968 edits the render-path classification and 926/927 the suites themselves — so it may run beside any of them, and it stands before the release because every point's landing waits on that gate), #1017 (1017 is the charge scoped to the lane it was first measured on, so the same red needs a second half on the other lane and nobody notices until a run stops — 603, 938 and 698 are three halves already written one at a time. It edits `scripts/render-verify-charges.mjs`, the same table 1015 narrows entry by entry, so it is worked with 1015 and never beside it), #1018 (1018 is the section a recorder knows and throws away, leaving the charge reader to parse it back out of text — refused twice by the cross-vendor round as syntax without provenance. It edits the run record's shape, which 1016's crash classification also writes, so the two are worked together and never beside each other), #1022, #1023 (1022 is the brevity guard that raised its own two ceilings in the same commit whose message says it granted no headroom, plus the line count that changes with a trailing newline; 1023 is the refusal that reprints its whole inventory, so the guard's message grows with the very debt it blocks on and the session closest to paying it has the least context left. Both edit a guard's own reporting rather than a suite, which no other open point of this bundle touches, so each may run beside any of them — but not beside each other, because 1023 rewrites the refusal text 1022's ceiling verdict is printed through), #1026 (1026 is the second backend pass LARGE runs on every change, including the ones that cannot touch the renderer — roughly 38 min of every run, hung on a predicate the picture check already uses. It edits `scripts/verify/tiers.mjs`, the tier map 595 and 926/927 read but do not write, so it may run beside any of them — but NOT beside 948, which edits the tier's own reporting, nor beside 909, which edits the verify runner's tier map; both write what this point rewrites; it stands AFTER the release tag because it narrows the very regression the tag is cut against), #1031 (1031 is the WebGPU lane's three ownerless red families — measured 31.08.2026 over a full 63-minute pass, every red put through the charge reader rather than read by eye. One of them is a CORRECTLY charged red the parser renamed: a check that printed no measurement keeps the separator in its name, so the anchored charge can never fire. It edits the red parser and the charge ledger that 1015, 1017 and 990 also reason about, so it is worked with them and never beside them, and it stands before the release because no picture verification can be read as covering while it holds), #1035 (1035 is the six findings the end-state cross-vendor reading raised against the charges 1031 landed — two of them excuse more than was measured: a timestamp row whose signature carries no cause, and two cascade alternatives that stop before the object name. It edits `scripts/render-verify-charges.mjs` and its cases, the same table 1017 and 1031 reason about, so it is worked with them and never beside them. It also shares `scripts/verify/baseline-classify.test.mjs` with point 1036 in Session- & Repo-Hygiene, so the two do not run in parallel either — written on BOTH sides, because the dispatcher reads only the entry of the point it is commissioning (point 1029). It also shares that table with 1015, which sits in Session- & Repo-Hygiene rather than here — the split follows shared files and this one crosses a bundle boundary, so the two must not run in parallel either, and saying so is the only thing that can keep the table's readers apart. It stands behind the release because the machine filed it) |
| **Dokumentation** | L | Docs and knowledge transfer | #303, #333, #422, #555, #645, #511, #512, #531, #607, #619, #794, #950, #980 (794 is the user's ruling of 20.08.2026 on the beginner guide's size budget — it edits the guide and its brevity ceilings, so it is not worked beside anything else that touches that document) — the rest landed 30.07.2026 (459), 950 (950 is the documentation cut that orphaned its own consistency guard: §7.1 lost the pointer lines the docs suite still searched for, and six rules reddened in every SMALL and LARGE run for five days. It enumerates every parser of `CLAUDE.md`, `design.md` and the work-order preamble and makes a cut that orphans one refuse at the commit; it edits the guide and doc budgets 794 also touches, so it is not worked beside it), 971 (971 is the delivered-hypothesis lesson that landed unenforced — the cross-vendor review of 27.08.2026 returned do-not-merge on the landed commit: a meta-rule demanding successful refutation where only a falsification attempt can be demanded, and an audit that pins none of the new claim's content — the review's third finding, a missing retrospective half, was refuted by measurement; it edits the guide and its brevity core that 794 and 950 also touch, so it is not worked beside them, and it stands behind the release because the machine filed it), 973 (973 is the cut-material lesson the guide still lacks, plus the currency attestation deliberately withheld on 27.08.2026 because two review findings stand against the file; it edits the guide and its brevity core that 971, 794 and 950 also touch, so it runs AFTER 971 lands and never beside it), 980 (980 is the retrospective handing the same `### 3.x` number to several lessons — eight numbers used more than once, so an outside citation like point 979’s “section 3.112” resolves to three different lessons; it renumbers and then GUARDS that document, which 794, 950, 971 and 973 edit as prose, so it is worked after them and never beside them) |
| **Steuerung & Performance** | M | Controls and performance | #310, #342, #347, #551, #618, #938 (938 is the streamed-dressing check that reds on the compatibility feature level while the point that owns it is ticked, so the red can be charged to nobody; it re-measures the fixed-anchor instance count on both feature levels and touches the flora streaming 310 and 618 also reach, so it is not worked beside them) |
| **Urlaubsfestigkeit** | N | Unattended operation for a fortnight — recovery from a failure at ANY moment, quota waiting, the boot path, the readiness check and the chaos drill that proves it | #442, #443, #444, #445, #446, #447, #448, #449, #450, #533, #562, #612, #504, #515, #517, #528, #616, #617, #658, #663, #716, #719, #760, #859, #861, #864, #865, #866, #867, #868, #956, #955, #964, #1002 (861 is the user's 23.08.2026 no-standstill order made standing: every state that parks the batch on a human gets a self-recovery lane or an own-judgment default with a retroactive-veto card, inventoried in docs/batch-autonomy.md — it reads the pause/gate/mandate paths this bundle owns and is worked after 859 and 860 supply its first two lanes; 859 is the standstill of 23.08.2026 that could not announce itself — a sick container hung every launcher tick for 3.5 h and every alert path needed a child process the container could not spawn; it edits the launcher daemon that 612, 760 and 1027 also own, so it is worked with them rather than beside them, and it is ranked FIRST by user order of 23.08.2026; 864-868 are the five code lanes 861's inventory named and could not build itself — 864 types the user gate so an advisory question decides itself, 865 makes a clockless pause prove it came from the user, 866 gives every spent failure ladder a capped probe instead of a person, 867 parks a point on a provider probe when a mandatory vendor allowance is out, and 868 re-arms the Windows launcher without an attended session; 864-866 stand before the release by the same 23.08.2026 order, 867 and 868 wait behind it), 944, 945, 947 (944 and 945 are the two measured halves of the 26.08.2026 two-hour standstill — the launcher spawning the owner inside a point worktree, and the successor decision vetoing resurrection beside a dead owner's live delegate while the watchdog counted refused ticks as failed spawns; 944 edits the spawn path 859 and 612 own and 945 edits the successor decision and watchdog counters 859 and 866 own, so both are worked with those, never beside them; 947 is the user-ordered blind four-eyes sweep of every remaining way the batch can stop, plus an independent double-safety fallback lane with its own timer — queued at the front by user order of 26.08.2026; its sweep may run any time, its fallback build is not worked beside 944/945), 958 (958 is the hole 947's own lane still leaves: the emergency decision counts a session's foreground tool calls as batch progress, so a wedge that keeps making calls — a poll loop, a retried guard refusal — never lets the clock run out and only the QUIET wedge is caught; it edits the decision core 947 built, so it never runs beside another point touching it, and it stands before the release by the 26.08.2026 no-standstill order), 955, 956 (955 is the unit gate that refuses while a delegated author commits — the Vitest teardown cannot tell test leakage into the live repository from a foreign branch's own progress, so every run that overlaps a busy lane goes red and the pre-push gate's single re-run pays a full suite for it; it edits the suite's own integrity check, which no other open point of this bundle touches, so it may run beside any of them. 956 is the owed half of a merge-with-fixes verdict, which the four-eyes gate clears by design and no guard tracks afterwards; it edits the review ledger's reader that 887 and 888 also reason about, so it is read after them and never worked beside them), 953 (953 is the CI run whose job never STARTED — conclusion null, zero steps, no runner — which `ci-status-guard` cannot classify and which point 711's in-step retry cannot reach by construction, so the turn end blocks until a human re-runs it. It edits the guard's remedy path and the watchdog workflow that 947 also sweeps, so it is worked with 947 rather than beside it), 964 (964 is the recovery block an early bail cut off from its own reachability on 21.08., the day it was built: only a DECLARED delegate can be freed, while an undeclared live checkout wedges the successor and nothing is left to end it — it edits the successor tick 945 also edits, so it is worked after it, never beside it), 966 (966 is the unit suite escaping its fixtures from a linked worktree — measured 27.08.2026 rewriting the live ref store during a push gate; it edits the suite's repository-integrity fence that 955 also owns, so it is worked with 955 and never beside it, and it stands before the release because every delegated push can repeat it), 985 (985 is the liveness probe that a reader's own look keeps alive: a read-only `git status` in a delegated worktree rewrites the index, and the probe stats that index as the agent's work output, so a dead author held an "alive" verdict for 21 minutes on 28.08.2026 and grew fresher with every check. It edits the same liveness core 958's emergency decision reads and 504's err-toward-alive tie-break owns, so it is worked with those and never beside them, and it stands before the release because it is the sensor the unattended batch waits on), 1002 (1002 is the honest long verification run that the narrowed emergency clock now reads as a wedge: the measured LARGE pass over both backends is 80m 48s against a 60-minute threshold, so a closing run takes a soft strike from the lane meant to protect it. It edits the decision core 947 built and 958 narrowed, so it never runs beside another point touching `scripts/batch-emergency-core.mjs`, and it stands before the release because it is a self-inflicted kill on the batch's own closing run), #1025 (1025 is the third measured way the successor veto stops the batch, after 945 and 964: the handover names its delegate in prose while `declaredAgentProbe` reads only `evidence[]`, so the owner's own transferable agent is read as a foreign live writer and vetoes the session that would adopt it — measured 31.08.2026 as 33 ownerless minutes over finished, pushed work. It edits the successor decision 945 owns and the declaration 964 reads, so it is worked after both and never beside them, and it stands before the release because every unattended handover takes this path), #1027 (1027 is the launcher's own record read as somebody else's process: `classifyDaemonRecord` compares the record's self-written start time with the OS start time for the same pid and calls a 2009 ms skew a recycled pid, so a daemon that needed longer than the two-second bound to write its record is disowned FOREVER and every context handover on that host is refused — measured 31.08.2026 against a demonstrably live daemon. It edits the launcher daemon's record and identity decision that 859, 612 and 760 also own, so it is worked with them and never beside them, and it stands before the release because it stops the one path that may never block), #1048 (1048 is the measured 02./03.09.2026 night crawl — an owner alive by heartbeat while waiting on stacked self-matching pgrep watchers that could never return, filed by user order for a blind four-eyes analysis, a third-model merge and the implementation of the merged fix; it reasons about the emergency/standstill decision core and the launcher's liveness reading that 947, 958, 985 and 1002 also edit, so it is not worked beside any of them) |
| **Kommunikation** | O | The communication PoC: the tonal language, who speaks it, where it is spoken, what the player may write down | #477, #478, #479, #480, #481, #482, #483, #484, #485, #486, #487, #488, #519, #659 (480 IS point 351 and 488 IS point 352, pulled forward — they close together); and the PLAY-SESSION findings of 09.08.2026: 587, 577, 586, 580, 582, 588, 576, 585, 584, 579, and of 10.08.2026: 605 |

Point **809** now supplies the Session- & Repo-Hygiene bundle's shared measurement
layer: one append-only journal and one lock-free report classify new wall time
before 811, 812, and 813 change the handover tick, writer veto, and CI wait. Its
first 14-day run leaves the pre-journal past unknown instead of attributing work
from commit gaps; the reproduced 21.08 incident measures 52 m 34 s of writer veto
and 6 m 02 s of scheduler transition without double-counting the CI trigger.

**Urlaubsfestigkeit** was cut on 30.07.2026 on the user's demand that the batch be
worked for two weeks without them, surviving an outage of Claude, of their internet
or of the machine at any moment — "auch mitten in einer kritischen Aktion". Two
decisions bound it: **no cloud worker** (so a dead machine or a fortnight-long
outage of the user's line stays an accepted residual — no local layer can cover it),
and **no pacing** — a quota block is retried until budget returns instead of being
spread out. Its order inside the bundle is 442 first (largest lever, smallest
change) and 449 last, because the drill is what makes the others more than a claim.

**Not bundled**, each for its own reason:

- **#184, #200, #203, #205, #207** — the big audits. They sweep the whole codebase and would
  swallow any bundle they were put in.
- **#174** — the release tag, gated on a full closing run rather than on a branch (224 stood here until the user withdrew it on 20.08.2026).
- **#635, #636, #637** — the release machinery the user asked for on 11.08.2026: the queue cut
  into release blocks, a block that closes and tags itself, and a board that can be
  rearranged by hand. Built under the assurance regime of 639.
- **#633** — the release's own closing run: two regressions with the blind-parallel cleanup
  between them. Not a bundle member; it IS the gate 174 waits on.
- **#887** — the four-eyes remainder that no reviewer can clear. It is a property of the
  review ledger and the authorship record rather than of any lane, it gates a point in
  another bundle, and putting it in one would tie an unrelated bundle's order to it.
- **#888** — the reviewer trailer that reads as authorship. Like 887 it lives in the
  review machinery rather than in any lane, and it changes a rule in CLAUDE.md §6, so
  every bundle's work would have to wait on a decision that is not that bundle's.
- **#977** — the four-eyes fallback that names a reviewer no command can start. Like 887 and
  888 it is a property of the review machinery rather than of any lane, and every bundle would
  otherwise wait on a gate that is not its own.
- **#285**.
- **#393** — sequenced behind 264, so it moves with that point rather than with a
  bundle.
- **#529** — confined to the protected user configuration outside the repository.
- **#591** — checks rule compliance across the whole repository.
- **#602** — inventories unused mechanisms repository-wide.

## Order of work

**THE RULE IS THE GOAL, NOT THE LIST** (user 04.08.2026): what ranks first is the
communication PoC being FINISHED as fast as possible. That pulls forward not only
its own twelve points but anything that raises the rate at which they can be
worked — the second backend lane (493) is the first such case, because without it
every picture check of the feature crawls. A point that makes the PoC land sooner
belongs at the top even when it is not part of the PoC.

**Kommunikation first** (user 03.08.2026, restated 04.08.2026): the communication
PoC outranks the whole queue — it is the feature the game is being built toward,
and the user asked for it before everything else. Its own build order is the wave
plan in TASKS.md (wave 1: 477 · 482 · 479), chosen so no two parallel agents own
the same file. **493 runs alongside it**, not after: the second backend lane is
what lets the wave's render points be merged under the both-backend rule at all.
THIS PARAGRAPH IS THE QUEUE'S ORDER — when it disagreed with TASKS.md between the
03. and the 04.08.2026, the queue kept feeding infrastructure while the point the
user had put first sat at position 60 (that is what the flat list is read as).

**URLAUBSFESTIGKEIT NOW LEADS, AHEAD OF THE PLAY SESSION** (user 10.08.2026: "Das darf
niemals passieren. Ich muss mehrere Tage am Stück weg sein dürfen und mich darauf verlassen
können, dass die Batch abgearbeitet wird."). The batch had stood idle for half an hour that
morning with nothing broken — a correct handover that no successor picked up — and the
bundle meant to prevent exactly that was sitting behind everything, because the flat list in
TASKS.md never carried the ranking this file declares. So the queue now opens with
562 · 533 · 612 · 448 · 449, and 613 rides with them because it blocks every delegating turn.
The 09.08.2026 play-session findings follow IMMEDIATELY behind and keep their precedence over
the rest — an unattended batch that cannot run works on nothing at all, so this is a
prerequisite for that work rather than a replacement of it. If the user wants the play-session
defects back in front, that is his call and the board asks it.

**THE 09.08.2026 PLAY SESSION OUTRANKS EVERYTHING, AND ITS MERGES ARE BATCHED**
(user 09.08.2026). The user played the deployed build and reported thirteen defects
and two extensions, almost all of them in the communication PoC. They lead the queue,
ahead of every other bundle including the infrastructure above — his words: "vor allem
anderen in der Queue".

He also asked for them to be worked and TESTED TOGETHER rather than one by one, and
that is taken the way this document already settled on 30.07.2026, NOT by putting the
bundle on one branch (point 471's rule stands: one branch per point) and NOT by
grouping the queue (point 472 took that back out the same evening). It is taken at the
MERGE, which is where the only real saving of the scheme sits: the finished per-point
branches of a package are merged TOGETHER and ONE both-backend regression runs over
the merged result. Five acceptance runs instead of thirteen, without reopening either
decision.

The packages, cut by what ONE acceptance run can judge — re-cut them as further
reports arrive rather than letting a package grow past its own acceptance:

**FIVE THROUGHPUT POINTS OVERTAKE THE COMMUNICATION BUGS** (user 10.08.2026, his
reasoning: a lever that makes every following point cheaper may well deliver the deferred
bugs EARLIER, not later). Ranked by their own measured shares, the head of the queue is
now 604 (the fatal one, already in flight), then **593 → 594 → 592 → 595 → 598**, and the
09.08. bugs follow.

- **593** first because it is the cheapest thing on the list — one binding paragraph in
  two prompts — and it pays from the next agent onwards: search and read alone are 25.1 %
  of the weighted spend, and 15.2 % of all output re-read what could not have changed.
- **594** because bookkeeping is 26.0 % of the weighted spend and 37.5 % of the machine
  hours, and it falls on the MAIN session — the one serial point every other point passes
  through, which spends 62.3 % of its own cost on it.
- **592** because waiting is the largest single lever measured: 10.9 % polling plus 3.6 %
  idling, ≈ 18.7 machine-hours in the measured window.
- **595** because verification is 47 % of the cost and the ladder bites exactly where the
  deferred bugs bleed — the render points that need a picture on both backends.
- **598** immediately after 595, not on its own merit (≈ 2 % of a median point) but
  because it is what ROUTES the ladder to the agents: 595's cheapest rung exists today and
  no brief mentions it, which is how it stayed unused for a month.

Left where they are: **597** (bounded output — real and compounding, the next candidate
if this batch pays off), **596** (it reduces the variance of the tail, not the average)
and **599** (pure measurement — it judges the others, it saves nothing itself).

**FOUR OF THE 09.08. POINTS DROP BEHIND 602** (user 10.08.2026): 581 (the faint
settlement boundary), 601 (Ctrl+W closing the browser), 600 (the unlabelled attacking
lion) and 603 (the ground's micro-detail) now sit AFTER 602. His reason, and it is the
ranking rule of this document applied by him: none of them is needed for the
communication mechanic, and none of them makes a following point cheaper — so neither
of the two things that pull work forward applies to them.

**AND THE 10.08.2026 SESSION LEADS THAT** (user 10.08.2026). He played the deployed
build again and reported two things. Being STUCK (604) goes to the front of the whole
queue, ahead of the 09.08. packages: with saving tied to port visits, a wedged traveller
loses the expedition, and no other defect on the list can end a session that way. The
speech volume (605) joins Ton, where it belongs — it is the same complaint as 577 seen
from the player's side, the control being unfindable rather than absent.

| Package | Points | The one acceptance |
|---|---|---|
| Festhängen | 604 | one walk into the reported wedge |
| Ton | 577, 587, 605 | one listening pass |
| Lehrtext erreicht den Spieler | 580, 582, 586, 588 | one live session in the village |
| Figuren | 576, 578 | one picture check |
| Ufer & Welt | 583, 584, 585, 581 | one walk along the bank and the boundary |
| Journal | 579 | HUD only, no scene |

Festhängen leads, then Ton: a session that cannot be continued makes every other
judgment moot, and after it, while the syllables are a squawk and the speech sits behind
a control nobody finds, nothing about the language can be judged at all.

**THE RANKING AS IT STANDS (user 10.08.2026).** The goal is the communication PoC in
a usable state and then **v0.3**, and the order serves that goal:

1. **Throughput and token cost first** — anything that measurably lowers what a point
   costs or raises the rate at which the queue is worked, including the measured batch
   STALLS out of Urlaubsfestigkeit (a batch that stands still has throughput zero).
   The remaining absence-hardening is insurance rather than a lever and follows later.
2. **The communication mechanic**, until the PoC is usable — that is what the release
   is for.
3. **The critical bugs**: anything that ends the player's session, loses the
   expedition, or voids a verification.
4. **v0.3 with the full closing** (dead code, stale docs and the `.md` audit included).
   It is gated on 2 and 3 ONLY. Features do not gate it.
5. **Everything else** — the visible-defect bundles, **Tierverhalten → Sonne & Himmel →
   Steuerung & Performance → Dokumentation** — and the big audits last.

Infrastructure leads because every later bundle is verified through it: the board
must tell the truth, the session handover must hold, the guard chain must actually
fire, and a red suite must mean a defect rather than machine load. Fixing those
first pays for itself in every bundle after — and the night of 29.07.2026 lost
hours to exactly those four failing at once.
