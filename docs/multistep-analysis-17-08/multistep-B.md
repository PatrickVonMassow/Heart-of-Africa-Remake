B1 | CLAUDE.md §6; scripts/point-brief.mjs, worktree-bootstrap.mjs, focus.mjs, board-publish.mjs | Point start is hand-driven as brief generation → feature branch/worktree creation → agent brief handoff → board focus/publish → first mutation; every transition adds calls and board-first can refuse the mutation after setup, so one `start-point <N>` should verify revision, queue order, branch/worktree, brief and board state, then launch the author.
B2 | CLAUDE.md §6 “Feature-branch process rules”; scripts/tasks-source.mjs, tasks-spec-guard.mjs, queue-order-guard.mjs, board-publish.mjs | Adding a point requires append on `main` → validate final-state specification and order → publish the changed open-point set → focus/confirm before work; late guards can consume blocked turns, so one `add-point` step should validate once and atomically update TASKS plus the board.
B3 | CLAUDE.md §6 “Feature-branch workflow”; scripts/commit-scope-guard.mjs, mechanism-review.mjs, mechanism-review-guard.mjs, model-trailer-gate.mjs | A cross-cutting main change requires decide that it is “small” → edit → obtain other-model review if it changes a mechanism → record review → construct a model trailer → commit/push; failures appear at commit or turn end, so one `land-infra-change` should verify scope, review and attribution before committing and pushing.
B4 | CLAUDE.md §6 “Feature-branch workflow”; scripts/model-trailer-gate.mjs, pre-push-gate.mjs, ci-status-guard.mjs | Every ordinary checkpoint is edit → test → compose an attributed commit → commit → immediately push → wait for concluded CI → clear the turn-end guard; this repeats network and guard calls per commit, so one `checkpoint` should prevalidate tests, scope and trailer, then commit, push and wait for the one resulting SHA.
B5 | CLAUDE.md §6 rescue rule; scripts/model-trailer-gate.mjs, pre-push-gate.mjs | A rescue checkpoint requires manually coordinate `[skip ci]` in the subject → `Rescue:` trailer → author-model trailer → commit → push, while the hook rejects any missing half only after commit is attempted; one `rescue-checkpoint --reason …` should construct and verify all metadata once, then commit and push.
B6 | CLAUDE.md §6 “Keep branches SHORT”; scripts/pre-push-gate.mjs | A stale branch requires fetch/update main → merge main into the branch → resolve conflicts → rerun relevant verification → commit/push the merge → perform final verification again; conflicts and tests consume repeated calls, so one `sync-feature` should verify divergence, perform the merge, identify conflict-touched code and run exactly the required gate before pushing.
B7 | CLAUDE.md §6 model policy and delegation; scripts/review-sol.mjs, point-proof-guard.mjs, render-verify-guard.mjs, land-point.mjs | Completing a point is author push → other-vendor review → rework/re-review if needed → reviewer-run suites → picture judgment where applicable → proof recording → landing; each downstream guard can reject an already-costly turn, so one `finish-point` orchestrator should verify the branch’s author/reviewer/proof requirements once and then land it.
B8 | CLAUDE.md §§6, 7.2 backend-sensitive picture verification; scripts/render-verify-core.mjs, render-verify-recorder.mjs, render-verify-state.mjs, render-verify-guard.mjs | Render verification requires classify changed paths → start the required backend → navigate/capture/judge → record it → repeat on the second backend when sensitive → satisfy the Stop guard; this duplicates browser and recording calls and blocks late if one record is absent, so one `verify-render-change` should calculate coverage once, run all required inspections and atomically record the verdicts.
B9 | CLAUDE.md §6 landing and point boundary; scripts/land-point.mjs, batch-boundary.mjs | Even though landing itself is bundled, the operator must run `land-point` → wait for merge/gate/archive/push/publish/cleanup → separately begin the mandatory boundary protocol; omission is discovered at answer/stop, so a single `land-and-handover` should verify landing success and immediately execute the sealed boundary transaction.
B10 | CLAUDE.md §6 “Context boundary”; scripts/batch-boundary.mjs, batch-boundary-core.mjs | A normal boundary is `--prepare <point>` → inspect and perform every bookkeeping action named by its output → `--commit <point>` last → stop; output-dependent manual commands cost tokens and an out-of-order mutation is denied, so one `batch-boundary --complete <point>` should verify all prerequisites once, perform the bookkeeping and seal the marker atomically.
B11 | CLAUDE.md §6 context watermark; scripts/context-watermark.mjs, batch-boundary.mjs, board-publish.mjs | At 150k tokens the owner must measure/detect the watermark → prepare a `--context` handover → update the board reason → drain unpushed work → commit the boundary → stop; detection after work can force another full-context turn, so one pre-call `context-safe-action` should verify remaining context and either execute the action or complete the context handover.
B12 | CLAUDE.md §6 in-flight transfer; scripts/batch-in-flight.mjs, batch-boundary.mjs | Handover with pushed checkpoints requires enumerate/record in-flight work → seal the boundary → successor claims ownership → successor separately runs `batch-in-flight --adopt`; missing adoption leaves a carrier for a Stop guard, so one successor-start step should verify the sealed transfer and atomically claim plus adopt all carriers.
B13 | CLAUDE.md §6 ownership lease; scripts/batch-lease-core.mjs, lock-heartbeat-hook.mjs, batch-owner-work.mjs | The owner must renew the lease before each governed call → make the call → repeat before the next call, and an expiry makes later merge/push/tick/publish writes fail; this is pure repeated guard traffic, so every governed operation should be one lease-wrapped action that verifies and renews ownership once immediately before acting.
B14 | CLAUDE.md §6 “THE WAY BACK”; scripts/batch-claim.mjs, batch-claim-core.mjs, batch-lock.mjs | A returning window issues a claim → waits for the current owner’s next hook → owner waits for merge/agent/verification quiescence → releases → claimant reruns/takes the claim before expiry; this spans blocked/waiting turns, so one supervised `claim-and-transfer` should verify quiescence and expiry continuously, then perform release and acquisition atomically.
B15 | CLAUDE.md §6 maximal delegation; scripts/board-queue.mjs, queue-rank.mjs, batch-owner-work.mjs | Maintaining the three-agent target requires inspect queue → identify non-overlapping points → inspect free slots → launch each worktree agent or manually provide `--slots-free` reasons → repeat after completion; repeated queue/context reads cost tokens, so one `fill-author-pool` should verify independence and capacity once and launch the maximal safe set.
B16 | CLAUDE.md §7.2 board-first rule; scripts/focus.mjs, board-publish.mjs, board-first-guard.mjs | Before the first mutation the session must set/confirm focus → publish if stale or `publishDue` → allow the mutation, while the PreToolUse guard otherwise refuses that mutation and forces a new turn; one `board-ready <action>` should verify focus, open-point set and publication once, repair them, then execute the requested action.
B17 | CLAUDE.md §7.2 BOARD family; scripts/dashboard-sync.mjs, dashboard-structure-core.mjs, dashboard-conciseness-guard.mjs, dashboard-card-topic-guard.mjs, dashboard-integrity-guard.mjs, dashboard-publish.mjs | Board upkeep is edit/sync state → separately satisfy structure, conciseness, one-topic, decision-card and real-state consistency checks → publish → verify the page; any late guard can demand another edit/publish cycle, so one `reconcile-board` should run every pure validation before a single atomic publish.
B18 | CLAUDE.md §§6, 7.2 deployed user decisions; scripts/decision-card-guard.mjs, dashboard-card-topic-guard.mjs, dashboard-publish.mjs | Asking the user requires create a single-topic decision card → ensure it references deployed main → publish → verify publication → ask; a missing or malformed card blocks only at turn end, so one `ask-deployed-decision` should verify deployment and card semantics, publish once and emit the question.
B19 | CLAUDE.md §7.2 “Ask the guards BEFORE the action, and answer LAST”; scripts/guard-preflight.mjs, closing-reply-core.mjs | Ending a turn requires focus confirmation → board publish/attest → boundary work → `guard-preflight --for answer` → only then compose the answer; each remedy is a separate command and a remaining guard wastes the reply turn, so one `prepare-answer` should reconcile every end-state family once and return a single authorization to answer.
B20 | CLAUDE.md §7.2 guard preflight; scripts/guard-preflight.mjs and each governed guard | Every governed action currently calls advisory preflight → interprets its remedies → runs N remedy commands → retries preflight → performs the still-separately-guarded action; duplicated checks and remedy loops consume turns, so each action should have one transactional wrapper that verifies all guards, applies safe remedies and acts under the same snapshot.
B21 | CLAUDE.md §7.2 CI-status rule; scripts/pre-push-gate.mjs, push-arrival-guard.mjs, ci-status-guard.mjs, ci-gate-verdict.mjs | A push is followed by arrival detection → workflow discovery for every pushed ref → wait for conclusion → inspect verdict → satisfy the Stop guard, and every extra push creates another watched SHA; one `push-and-settle` should verify local eligibility, push once, track every created ref and return only a final aggregate verdict.
B22 | CLAUDE.md §7.2 red-CI rules; scripts/ci-status-guard.mjs, ci-failure-cause-core.mjs, alert-escalation.mjs | Red CI produces guard refusal → separate failure inspection → cause naming → fix/charge/open-point decision → new commit/push → new CI wait → guard retry; the refusal itself yields nothing, so one `resolve-ci-run` should verify the cause disposition before accepting the repair push and then follow it to a conclusive green or durable open item.
B23 | CLAUDE.md §7.2 FINDING family; scripts/finding.mjs, findings-request-core.mjs, findings-guard.mjs | An investigation can run → reach an answer with no durable artifact → be blocked by the finding guard → create a finding afterward → retry the answer; this pays a full context for a predictable condition, so one `finish-investigation` should verify whether the turn produced a durable result and create/close the finding before answer authorization.
B24 | CLAUDE.md §7.2 finding carrier rule; scripts/findings-core.mjs, findings-guard.mjs | A carrier is created/transferred → owner later discovers it at Stop → separately drains or reassigns it → retries Stop; the check is too late, so one owner-work wrapper should verify and drain all owned carriers before allowing unrelated work or an answer.
B25 | CLAUDE.md §7.2 retrospective currency; scripts/retro-currency-guard.mjs, retro-refresh.mjs, retro-sources.mjs | Work proceeds → retrospective becomes stale → Stop blocks → sources are gathered → retrospective refreshed → answer retried; this is an after-the-fact full-turn penalty, so one `ensure-retro-current` should verify source revisions and refresh once before the first closing action.
B26 | CLAUDE.md §6 TASKS/archive rules; scripts/tasks-source.mjs, tasks-archive-guard.mjs, tasks-spec-guard.mjs, queue-order-guard.mjs | Nonstandard task maintenance requires edit TASKS → move closed text verbatim to archive or restore open text → check duplicates/state/order/final-state wording → publish the changed queue; separate late guards multiply repair cycles, so one `edit-work-order` should validate the proposed pair of files once and atomically write plus publish them.
B27 | CLAUDE.md §7.1 acceptance-document rules | Changing a criterion requires edit its short CLAUDE entry → edit the same-number detail section → edit the same-number evidence section → keep all three in one commit; omission is detected only in later tests/review, so one criterion-update step should verify number/link parity and apply the coordinated change as one act.
B28 | CLAUDE.md §§1, 4 | A user-requested design change requires determine whether it touches design content/build order → update `design.md` → update CLAUDE.md → update code → commit them together; repeated document reads and partial-state guards cost context, so one change-scaffold step should verify affected authoritative sections once and stage every required surface.
B29 | CLAUDE.md §6 Language and Voice markup | Each journal/UI text change requires author English → author contextual German → add emotional markup to both journal variants → verify display stripping and speech parsing; omissions emerge in tests after editing, so one localization-entry operation should validate both languages and markup before installing the pair.
B30 | CLAUDE.md §§5, 7.2; scripts/audit-check.mjs | Major-system verification is build → lint → conditional audit if lockfile moved → unit tests → selected browser gate → core-view screenshot checks, often followed later by LARGE; separate commands repeat startup and allow a late layer to invalidate the whole turn, so one change-aware `verify-once` should detect paths/lockfile/backend sensitivity and execute the exact required matrix with one verdict.
B31 | CLAUDE.md §7.2 frame-subject rule; scripts/verify/frameSubject.mjs | A screenshot requires declare subject/type/reason → navigate to a supposedly achievable state → project the subject at shutter time → capture → have the writer refuse it if absent; failure occurs after browser setup and navigation, so one verified shutter action should resolve and project the subject first, then capture only under that same frame state.
B32 | CLAUDE.md §7.2 “A red closes by a CAUSE”; scripts/render-verify-charges.mjs, throttle-probe.mjs | A red browser run requires retain retry → investigate cause → choose fix, charge, open point or one-run defer → possibly run an eight-run throttle probe → record the disposition; retries alone cover nothing and each choice is another command, so one `adjudicate-red` should verify evidence and atomically record exactly one valid closure path.
B33 | CLAUDE.md §9 Closing freeze; scripts/batch-in-flight.mjs, batch-owner-work.mjs | Starting closing requires stop spawning → enumerate live agents → let each finish or park → drain/transfer carriers → verify no merge or work can land → begin closing; races restart the closing, so one `enter-closing-freeze` should verify quiescence and atomically freeze landing/spawning before returning success.
B34 | CLAUDE.md §9 closing completeness; scripts/closing-guard.mjs, closing-guard-core.mjs | Closing is first LARGE → launch two blind cleanup authors → third-model semantic merge/accounting → commit cleanup → second LARGE on the final commit → record ordered evidence → request tag approval; this is the largest hand-driven chain and any missed order blocks release, so one resumable `run-closing` should verify the frozen SHA at each boundary and drive the complete state machine.
B35 | CLAUDE.md §6 four-eyes principle; scripts/blind-merge.mjs, mechanism-review.mjs | A divergent stage requires prepare identical inputs → launch two blind runs → ensure every entry has an ID → select a third, nonauthor merger → merge by meaning → account for every A/B ID → record merger identity; omissions vanish silently or are rejected late, so one `run-divergent-stage` should verify blindness, identities and complete accounting before accepting the merged artifact.
B36 | CLAUDE.md §§6, 7.2 mechanism review; scripts/mechanism-review.mjs, mechanism-review-guard.mjs, rule-review.mjs | A guard/gate/hook change requires implement → hand artifact to the other model → receive review → record model and verdict → satisfy the Stop guard; forgetting the record wastes the completed turn, so one `review-and-record-mechanism` should verify reviewer independence and atomically attach the review to the changed mechanism.
B37 | CLAUDE.md §6 model policy; scripts/author-routing-core.mjs, author-sol.mjs, review-sol.mjs, criticality-review-guard.mjs | Point execution requires classify difficulty/verification-as-work → route to Sol or Claude/Fable → launch author → detect escalation after Sol rework → launch the permitted other-vendor reviewer → record roles; misrouting is discovered by guards after expensive authoring, so one `route-and-run-point` should verify criticality and role independence once before launching either role.
B38 | CLAUDE.md §6 release/tag mechanism; scripts/closing-guard.mjs, deploy-staleness.mjs | Release is verify closing for the exact commit → obtain explicit approval for the specific version → create `vX.Y` → move `poc` to the same SHA → push both tags → dispatch deployment or arrange a later main push → verify both URLs; repeated outward writes can leave split state, so one approval-gated `release-version` should reverify the approved SHA once and perform the tag/deploy transaction.
B39 | CLAUDE.md §6 release deployment; scripts/deploy-staleness.mjs, pages-deploy-unblock.mjs, check-deployed-benchmark.mjs | Because tag pushes do not deploy, the operator must push tags → manually dispatch Pages → wait → test `/poc/` → test `/vX.Y/` → unblock/retry if stale; each network phase can consume a turn, so one `deploy-and-verify-release` should dispatch once, follow the run and validate both immutable expected SHAs before success.
B40 | CLAUDE.md §9 closing guard | A TASKS point that delivers a version cannot be ticked through normal landing until every closing step is separately recorded for that commit → tag approval obtained → release performed → point ticked; the special dependency can make `land-point` stop late, so one release-point landing operation should verify closing evidence and approval before beginning merge/tick state changes.
B41 | CLAUDE.md §6 deployed-main aesthetic judgment; scripts/land-point.mjs, dashboard-publish.mjs, deploy-staleness.mjs | A render fix needing aesthetic feedback requires local two-backend verification → land to main → push/wait CI → wait for Pages → verify deployed state → create/publish a decision card → ask the user; this spans several costly waits, so one `land-deploy-and-request-judgment` should verify correctness once, follow deployment and publish the URL-bound question automatically.
B42 | CLAUDE.md §6 branch cleanup; scripts/branch-hygiene-guard.mjs, worktree-cleanup.mjs, land-cleanup-core.mjs | When normal cleanup is missed, Stop discovers a merged branch → operator identifies containment → removes worktree → deletes local branch → deletes remote branch → retries the guard; several destructive targets must be resolved by hand, so one `prune-landed-branch` should verify exact SHA containment once and remove all three artifacts safely.
B43 | CLAUDE.md §6 model allowlist; scripts/model-guard.mjs, model-trailer-gate.mjs | An unnamed author can commit → model guard blocks at turn end → transcripts are searched to resolve identity → commit metadata is repaired or work paused → guard retried; this spends a full context after authorship is already knowable, so one commit wrapper should resolve and validate the author model before accepting the commit.
B44 | CLAUDE.md §7.2 document ceilings; scripts/doc-budget-core.mjs, doc-budget-guard.mjs | Documentation is edited → Stop measures corpus ceilings → guard rejects an oversized result → author reopens and compresses/splits it → retries; the size check runs too late, so one doc-update operation should calculate the proposed budget and required destination before applying the edit.
B45 | CLAUDE.md §6 point briefs; scripts/point-brief.mjs, point-brief-core.mjs | Delegation requires generate brief → copy it into the prompt → agent discovers an unresolved/ambiguous reference or stale revision → escalates → main regenerates/augments → relaunches; this costs an agent turn, so one launch operation should verify revision and all references before creating the worktree agent.
B46 | CLAUDE.md §§5, 7.2 dependency hygiene; scripts/deps-preflight.mjs, audit-check.mjs | A dependency change requires justify necessity → modify lockfile → install/wire hooks → build/lint/unit → audit → commit with justification; a late audit or hook failure discards prior work, so one `add-dependency` step should preflight availability/advisories and justification, then install and run the required verification once.
B47 | CLAUDE.md §9 graphics-detail confirmation; scripts/closing-guard-core.mjs and `src/config/qualityDoc.test.ts` named in CLAUDE.md | Closing runs unit tests that already prove quality-doc parity → manually confirms the same document again → separately records that closing evidence; duplicate verification costs calls and tokens, so the closing recorder should consume the unit-test proof automatically and record the confirmation once.
B48 | CLAUDE.md §7.2 no-idle-wait rule; scripts/wait-marker.mjs, ci-status-guard.mjs, batch-progress-guard.mjs | Waiting work requires establish a legitimate wait marker → poll the external job → clear/update the marker → resume → pass the no-idle-stop guard; mishandling produces a blocked stop, so one supervised wait operation should verify the awaited object, maintain the marker and resume automatically at conclusion.
B49 | CLAUDE.md §9 closing evidence ordering; scripts/closing-guard.mjs, closing-guard-core.mjs | The operator runs `--status` → performs a closing step → manually crafts `--step <id> --evidence …` → repeats for every step → finally attempts the tag, while malformed or temporally stale evidence is rejected late; one closing runner should capture SHA/time/tool verdicts directly and record each step under the same verified state.
B50 | CLAUDE.md §6 chat return path; scripts/chat-inbox.mjs, chat-watcher.mjs, batch-claim.mjs, batch-launcher.mjs | A message-driven return requires launcher poll → verify untrusted inbox input → establish bounded claim → ensure no owner/honoured claimant → spawn responder → hand back or release ownership; separate watcher/claim/launcher states invite expired claims and extra turns, so one message-dispatch transaction should verify authorization and ownership once before spawning the responder.
B51 | CLAUDE.md §7.2 batch timestamp/chat currency; scripts/timestamp-guard.mjs, chat-core.mjs, chat-delivery-core.mjs | Work can finish → timestamp guard notices a newer chat event or stale reply state → session rereads/verifies inbox → updates reply/delivery timestamp → retries answer; this blocks after the response work, so `prepare-answer` should atomically reconcile the verified latest chat timestamp before authorizing output.
B52 | CLAUDE.md §7.2 guard health; scripts/guard-health-guard.mjs, guard-inventory.mjs, guard-hooks.test.mjs | A mechanism change can be reviewed and committed → inventory/hook wiring is checked separately → guard health reports an unwired or drifting guard at Stop → wiring/test/review cycles repeat; one mechanism installation step should verify inventory, hook registration, fail-open behavior and review before committing.
B53 | CLAUDE.md §7.2 render red charging; scripts/render-verify-charges.mjs, findings-guard.mjs, tasks-source.mjs | To charge a failure, the owner must identify the red run → select its owning open point → write the charge → ensure that point exists in TASKS/archive sources → satisfy both render and finding guards; invalid ownership is discovered after recording, so one `charge-red` should verify the target point and atomically attach the evidence.
B54 | CLAUDE.md §6 user approval and deferral; scripts/defer-for-user.mjs, decision-card-guard.mjs, dashboard-publish.mjs | Deferring a matter to the user requires create the durable deferral → create a decision card → ensure one-topic/deployed-state compliance → publish → then stop or ask; separate artifacts can drift and trigger multiple guards, so one `defer-to-user` should validate and publish the linked deferral/card in a single step.
B55 | CLAUDE.md §6 direct-main arming exception; scripts/enable-hooks.mjs, prep-arm-hook.mjs, prep-guard.mjs | Arming a new setting or git hook requires attended edit → enable/install hook → verify prep state → run hook tests → obtain mechanism review → record it → commit/push; late prep or review guards waste the attended turn, so one `arm-mechanism` should verify wiring, tests and independent review before applying the attended mutation.
B56 | CLAUDE.md §7.2 backend routing; scripts/verify/tiers.mjs and README named in CLAUDE.md | Changing suite routing requires edit the tier map → edit README in parallel → update/run tier unit tests → run the selected backend matrix; mismatch is found only after separate tests, so one tier-map editor should validate suite/backend invariants and generate both code and documentation from one verified specification.
B57 | CLAUDE.md §7.2 pushed-ref coverage; scripts/ci-status-guard.mjs, push-arrival-guard.mjs | Multiple commits pushed during one task create multiple refs/SHAs → each is discovered separately → each workflow is awaited → every verdict must conclude green before answer; repeated polling scales with network writes, so one push batch should verify the complete intended ref set, publish it once and await a single aggregated CI barrier.
B58 | CLAUDE.md §6 open-item rule; scripts/finding.mjs, tasks-spec-guard.mjs, board-publish.mjs | Unclear design requires add `// OPEN` in code → create the corresponding end-of-run/open-work item → ensure final-state wording/order → publish the board; forgetting the durable counterpart blocks or loses the issue, so one `record-open-item` should verify ambiguity and atomically create the code marker, work-order entry and board card.
