A1 | scripts/board.mjs | The board couples a card edit with a publish: the edit helper applies the change, rotates the archive and publishes in ONE step, with no edit-only mode, so every guard remedy produces another publish. Measured 17.08.2026: ~12 publishes in 50 minutes, which drove raw.githubusercontent into HTTP 429 and took the user's board offline. One staged mode should let several edits accumulate and publish once.
A2 | scripts/board-queue-core.mjs (parseSetArgs) | `set` swallows a positional text that follows a flag: --title/--estimate switch a MODE, so `set 702 --estimate "~2 h" "<prose>"` puts the prose into the estimate bucket where normalisation discards it, and the command still reports success. Result: three calls per card instead of the one the usage line advertises. An argument the processing discards must be refused loudly.
A3 | scripts/guard-preflight.mjs | The preflight cannot judge four wired guards (ci-status, timestamp, decision-card, batch-progress), and those four then block the turn end one after another; a blocked turn produces nothing. Measured 17.08.2026: the batch-progress-guard loop alone cost seven replies at ~250k context each. The read-only report must cover what is coverable and name the clearing action for the rest.
A4 | scripts/batch-boundary.mjs | The boundary prints a board card it could place itself: it generates the text (boundaryCardText), imports PUBLISH_CMD and has execFileSync, but leaves placing and publishing to the caller. Measured: three refused --commit runs (card missing, card names no point, card byte-identical inside one minute) plus a conciseness re-put. One --complete should place, publish and seal.
A5 | TASKS.md, scripts/queue-rank.mjs, scripts/board-queue.mjs | Filing one point is ~10 hand-driven calls with no helper: append to TASKS.md, three guards, commit, push, three board-queue set calls, render, publish, queue-rank --ranked. No script in the tree appends a point, and queue-order-guard blocks the turn end when the ranking is forgotten, so the chain is mandatory and unbundled. Run three times on 17.08.2026.
A6 | scripts/author-sol.mjs, scripts/worktree-bootstrap.mjs | Commissioning a point is three hand calls with no helper: git worktree add -b feat/<n>-<slug>, worktree-bootstrap, author-sol --point; author-sol requires an EXISTING worktree and branch. The END of a point has land-point.mjs with 15 bundled steps; the START has nothing.
A7 | scripts/board.mjs (attest), scripts/focus.mjs, scripts/board-publish.mjs | The turn-end duty chain has no single command: focus set, board-publish, dashboard-guard --synced, board.mjs attest, guard-preflight. attest bundles three of them but neither the publish nor the focus, and batch-progress-guard alone names seven distinct commands across its remedies.
A8 | CLAUDE.md paragraph 6 and 9 vs scripts/land-point.mjs | The bundling is asymmetric by design history: the project bundled the RARE and dangerous END (land-point, 15 steps, "the landing is ONE command") and left every FREQUENT start, checkpoint and turn end hand-driven. Since cost scales with CALL COUNT times context, the frequent sequences are where the spend actually is.
A9 | the session transcript, measured 17.08.2026 | The cost mechanism itself is unmeasured in operation: this session paid 378 model calls at an average of 158,001 tokens of context per call (last 20 calls: 251,884), i.e. 58 million cached input tokens, while landing ZERO points. Every avoided tool call and every avoided blocked turn is worth roughly one full context, which is what makes bundling a cost lever rather than tidiness.
