A1 | src/state/store.ts:522 | 6 | must-do | The run starts with `shovel: 0`, so the player must buy a shovel before the upstream dig. Without one, the dig at the boulder only answers `digNoShovel` (store.ts:1931).
A2 | src/state/store.ts:493 | 1 | must-do | The default start is Cairo, and the only drum village is `bambara-village` (geo.ts:231). The player must travel across the continent to reach it before any of the chain exists.
A3 | src/scenes/place/chiefMeeting.ts | 4 | silent | Nothing in the game points the player to Bambara Village as the one village with a message. Every other village's chief walks out and only nods (`chiefNoMessage`), so the right village is found by chance.
A4 | src/i18n/en.ts:414 | 4 | break | The `chiefStepsOut` toast ("through the drums he will speak with me") fires in EVERY village (store.ts:793 checks only village kind). Outside Bambara it promises a message that never comes.
A5 | src/i18n/en.ts:1024 | 4 | break | The `chiefWalk` journal entry ("I was meant to follow") is written in every village on first step-out. It tells the player to follow the chief, while the actual act is to stand before him and the drummer.
A6 | src/state/store.ts:738 | 5 | silent | `receiveDrumMessage` records all four atoms as heard, with the drum village as their first place, even if the player never heard them from a villager. The glossary then shows words as "heard" that no lesson ever taught.
A7 | src/i18n/en.ts:1027 | 4 | break | The `drumMessage` journal says "I know these words. I have heard every one of them in the lanes and at the water" whether or not that is true. The chief speaks from the start, so a player who goes straight to him reads a false claim.
A8 | src/communication/drumMessage.ts:88 | 1 | silent | The drum message's peak is scaled by `balance.ambienceVolume`. A player who lowers ambience also silences the chief's message, but it is still recorded as heard and shown on paper.
A9 | src/systems/ambience.ts:1179 | 1 | silent | `playSpeech` returns silently when there is no audio context or master. `hearUtterance` and `speakOverhead` still fire, so the note and the journal entry appear with no sound.
A10 | src/scenes/place/PlaceLife.tsx:946 | 1 | silent | A word counts as heard from distance alone (`speechReach(distance).audible`), not from the level that reaches the output. A zero speech slider or a muted tab still records "heard" and raises the note.
A11 | src/scenes/place/PlaceLife.tsx:2392 | 1 | break | Village speech is not held while the drums beat. Only the drummer's arms react. Children's CALL words (34 m) and adult words can fall on top of the 16 strikes, and the master limiter (spec, point 1156) then squeezes both.
A12 | src/config/balance.ts:1780 | 1 | must-do | The player must stand within TALK reach (10 m, falloff 4 → 20% at the rim) of an adult or the weaver, or within CALL reach (34 m) of a child, to get the word, the note, and the E invitation. Beyond that the figure neither speaks nor gestures.
A13 | src/config/balance.ts:1753 | 3 | break | The legacy `hearingRadius: 10` is still the default reach in speechChannel.ts:129/170, beside `talk.reach` and `call.reach`. Any speaker registered without an explicit reach falls back to the wrong constant.
A14 | src/scenes/place/layout.ts:907 | 2 | must-do | Named cause 1 is judged closed in code. `playRocks` stand on the bank as an upstream/downstream pair (the old village-centre stone is gone), which matches spec rule "two rocks at the ends of a stretch of bank". The picture on both backends still needs to confirm it.
A15 | src/scenes/place/PlaceScene.tsx:1421 | 2 | silent | The two bank rocks are drawn identically, same size and mesh. Only the river's streak shader (PlaceScene.tsx:1966) tells upstream from downstream, so the "also observed" poor distinguishability stays open wherever the water is off-screen or the streaks are faint.
A16 | src/scenes/place/bankGame.ts:990 | 2 | break | The direction announcement points at the far rock (`gesture: 'point'`, aim = far rock). UPSTREAM/DOWNSTREAM can therefore be read as "over there / to the other rock", a reading only the loom can prune.
A17 | src/scenes/place/bankGame.ts:1014 | 2 | silent | When the catcher's walk to his stone is blocked, the tap and its ROCK are skipped. The run opens without the word, so a whole round can teach ROCK only as "arrived".
A18 | src/scenes/place/layout.ts:2122 | 2 | break | `climbRock` is null wherever `deriveClimbRock` finds no spot, and a cycle whose boulder is unreachable says nothing (balance.ts roundSilence note). ROCK then never transfers beyond the game's targets, which the upstream boulder depends on.
A19 | src/scenes/place/bankGame.ts:996 | 2 | must-do | The player must follow the children to the bank when one calls RIVER and points at the water, then watch several runs from a spot where both rocks and the flowing water are in view.
A20 | src/scenes/place/bankGame.ts:1001 | 2 | break | Named cause 3 is still open as code. Nothing consults the player's heard memory (no `hasHeard` use in scenes), and one bank run presents the direction word and then the tap's ROCK one hearing gap apart. The two unknowns arrive together.
A21 | src/scenes/place/adultWork.ts:30 | 2 | must-do | Named cause 2 is judged closed for the rebuilt catalogue. Water (full jar set down at the stand) and DIG (pit, post, or planted patch completed) end in a visible result. The play-through must show the jar and the finished work in frame.
A22 | src/config/balance.ts:1623 | 2 | break | `adultErrands.dwellSeconds: 6` is still in balance and in the debug menu (DebugMenu.tsx:231), but no code reads it (adultWork.ts:93 is only a type). It is a dead leftover of named cause 2 that points a tester at a knob that does nothing.
A23 | src/scenes/place/adultWork.ts:33 | 2 | break | The "fixed loop" observation still holds. The adult catalogue is exactly four situations (water-out, water-back, dig-first, dig-second) on a fixed interval grid, so the errands still read as a repeating loop.
A24 | src/scenes/place/adultWork.ts:13 | 2 | silent | Both adult words yield while any child can hear, and DIG is staged only with a second free adult clear of the children's grounds. With the children roaming near the stand, RIVER/DIG can be deferred up to `speechHoldSeconds` 240 s, and a waiting player sees idle adults.
A25 | src/scenes/place/loom.ts | 2 | break | The loom teaches UPSTREAM/DOWNSTREAM a second time only with a river and a helper, and a shipped plan can make it give way (`gaveWayToLoom`). If Bambara's station lacks either, the direction words keep only the ambiguous bank reading.
A26 | src/communication/speechFloor.ts | 2 | silent | Under one-situation-at-a-time, a word held past its hold is force-spoken and reported only as a dev defect. The player gets a word whose consequence window has already passed, heard but no longer tied to a visible act.
A27 | src/scenes/place/chiefMeeting.ts:48 | 5 | must-do | CHIEF is taught only when the player presses the use key at the drummer while the chief is in his hut. Once the chief is out, the same key sends the message instead, so the order of play decides whether CHIEF is ever heard.
A28 | src/scenes/place/speechChannel.ts:88 | 3 | silent | `dropFloorLabels` removes the TARGETED note too when any other floor word falls. A player walking up to press E loses the invitation mid-approach when the village speaks elsewhere.
A29 | src/config/balance.ts:1802 | 3 | must-do | The note stands 2.6 s (`labelSeconds`). The player must be within reach and get the speaker highlighted in that window, after which the target holds the note, to press E and open the guess dialog.
A30 | src/ui/SpeechGuess.tsx:37 | 3 | break | Guess drafts are keyed by utterance. A phrase with a repeated atom gets two inputs bound to one draft, and the last one typed wins silently.
A31 | src/state/store.ts:704 | 3 | silent | Every chronicle entry (drums heard, find, give, fit) sets `journalOpen` true unless do-not-disturb is on. The book opens over the scene in the same frame the drum dialog opens, so the paper may be hidden behind or cluttered by it.
A32 | src/ui/DrumMessage.tsx:107 | 4 | silent | The drum paper opens by itself only if no other dialog is open at the last beat. A player in the guess dialog then gets no paper, only a journal reopen button (JournalPanel.tsx:120) they must find.
A33 | src/state/store.ts:765 | 4 | must-do | To get the message, the player must press use at the hut, wait for the chief's walk across to the drummer, then press use at the chief or the drummer within `chiefStaySeconds`, and listen to all 16 strikes before the paper appears.
A34 | src/scenes/travel/Rivers.tsx:326 | 6 | must-do | In the bird's-eye view the player must tell upstream from the river's streaks, which scroll with the current, and follow the Niger 1.6–2.4° upstream (communicationRock.ts:27, about 3 days) to the lone tall block.
A35 | src/world/communicationRock.ts:44 | 6 | silent | Only the block's shape (3.2 units, dressing cleared) sets it apart. Nothing links it on screen to the village's play rocks, so the ROCK transfer must happen in the player's head without any seen feedback.
A36 | src/state/store.ts:1976 | 6 | must-do | The player must press dig in travel mode within `digRadius` 3 units (0.3°, balance.ts:1109) of the block centre. The find enters the bar as "Find from the Boulder", outside pack capacity.
A37 | src/i18n/en.ts:1029 | 6 | break | The `rockArtefact` journal entry supplies the translation: "followed the water against its own pull", "the kind of thing the children name at their own two rocks". This breaks the spec rule that nothing hands the player a translation.
A38 | src/state/store.ts:1976 | 6 | break | The dig at the boulder succeeds without the errand ever being heard (`drumMessageHeard.errand` is not checked). A player can shortcut the language chain entirely, so the chain cannot prove the words were used.
A39 | src/ui/Hud.tsx:168 | 4 | must-do | The give happens only by activating the find in the inventory bar (click or digit key) within `giveReach` 2.6 m of the chief standing out in the open (store.ts:819–824). Anywhere else the answer is the `findNeedsChief` toast.
A40 | src/i18n/en.ts:230 | 4 | silent | `findTooltip` names only "Click it before the chief". In the place scene the pointer is locked, so a click on the bar cannot reach it, and the working digit key is never named.
A41 | src/state/store.ts:844 | 4 | must-do | After the give, the answer RIVER · DOWNSTREAM is beaten at once beside the drummer, or on arrival if the chief is walking (`drumOnArrival`). The player must wait for its last beat for it to be recorded and shown.
A42 | src/state/store.ts:832 | 4 | silent | The clay form enters the bar in the same store write as the give, with no drawn hand-over, while the spec says "handing over the clay form wordlessly in the same move". Only a new bar slot and a journal line show it happened.
A43 | src/ui/DrumMessage.tsx:34 | 5 | must-do | Deciphering after the fact in either order works: the paper and the journal read and write the same memory. The player must reopen the messages from the journal and revise the readings once the direction words are understood.
A44 | src/world/data/landmarks.ts:113 | 6 | break | The Bandiagara socket (14.35, -3.4) lies about 0.8° east of the Niger near Mopti. RIVER · DOWNSTREAM leads down the river and past it, and no word tells the player to leave the river for the escarpment.
A45 | src/world/forms.ts:67 | 6 | must-do | The player must stand within 0.3° of the talus-foot landmark origin in travel mode and activate the clay form. Inside any settlement it only answers `formInSettlement` (store.ts:2027).
A46 | src/i18n/en.ts:1033 | 6 | break | The `mouldFitted` journal entry translates the answer ("followed the water the way it wants to go"), again against the spec's no-translation rule for the fit and opening.
A47 | src/state/store.ts:2050 | 6 | break | Fitting the form only sets the toast `pocSolved` ("Dummy message: the puzzle of this proof of concept is solved") and a journal page. `victory` is never set. The chain's end state is a transient placeholder toast, not the victory overlay (Hud.tsx:431), which only the grave dig raises (store.ts:1938).
A48 | src/i18n/en.ts:411 | 6 | silent | The toast is the only immediate sign of success at the socket and disappears after its timeout. A player looking at the scene or map at that moment can miss that the chain is complete.
A49 | src/world/forms.ts:104 | 6 | break | The socket distance is computed as a plain lat/lon hypot, with no cos(lat) correction. The same holds at the boulder (communicationRock.ts:137). The reach is an ellipse, not the drawn circle, so standing at the drawn spot can differ from what the check accepts.
A50 | src/state/store.ts:2158 | 4 | break | A resumed snapshot clears `chiefOutside` and the walk. A player who loads beside a chief standing out finds him back in his hut, and a give tried at once gets `findNeedsChief`.
