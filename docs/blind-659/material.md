# Blind-parallel material — work-order point 659

Both halves of the blind-parallel stage receive EXACTLY this input and nothing
else. Neither half sees the other's answer before it is committed.

Halves (user 24.09.2026, 07:43): A = Opus 5.5, B = GPT-6 Astra. Merger: Fable 5.1,
who writes neither list. A first run from 24.09.2026 was discarded unread.

Tree: branch `feat/659-communication-playthrough`, forked from `main` at e4d214360.
The point text is `docs/blind-659/point.txt` (verbatim from the work order).

## State note given to both halves

The point was put ON HOLD on 13.08.2026 until the communication rebuild landed.
That rebuild has since landed (archived points include 689, 1064, 1070, 1155,
1168, 1174: five-word vocabulary RIVER/UPSTREAM/DOWNSTREAM/ROCK/DIG rolled per
run, chief speaks from the start, children teach ROCK/UPSTREAM/DOWNSTREAM
through a game between two bank rocks, chief answers RIVER · DOWNSTREAM on the
drums plus a mould/impression). The CODE is authoritative for what the chain is
now; the point's three "named causes" were written against the pre-rebuild
state and may be closed, changed or still open — judge them against the code.

## The question put to both halves

> You are ONE HALF of a blind-parallel six-eyes analysis (the ENUMERATING half).
> Independently, without knowing what the other half writes, enumerate for the
> Heart of Africa communication chain as it exists in THIS tree:
>   (a) what can break,
>   (b) what a player must be able to do at each step to get from entering the
>       village to the victory state,
>   (c) which step could silently NOT ARRIVE at the player — works internally
>       (state, event, call made) but is not seen, heard or read.
> Cover the six chain stages of the point (hearing the speech; recognising
> what the children's game and the adults' errands express; the guess/label/
> dialog/journal; the chief, gift, reply, hint; direction words, glossary,
> retroactive deciphering in either order; search, excavation, return, victory)
> and re-judge the three named causes and the two "also observed" items
> against the current code.
>
> Read the code (start at src/communication/, the village/errand/children/
> chief/goal code it leads to, src/config/balance.ts, the en/de language
> files) and docs/communication-poc-spec.md. Do NOT run browser suites, do
> NOT edit, commit or push anything.
>
> Deliver ONLY a numbered list in the line form, one entry per line, each
> discrete and independently mergeable:
>   `<P>1 | <file or file:line> | <stage 1-6> | <kind: break|must-do|silent> | <one line: what, and why it matters to the player>`
> where <P> is your half's prefix (A or B). 25-60 entries. No prose before or after.
